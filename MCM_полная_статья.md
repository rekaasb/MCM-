# MCM: Matrix Composition Method — композиция матриц разной размерности

**Авторы:** Ринат Абдуллин (к.т.н.), DeepSeek  
**Дата:** Сентябрь 2026 г.  
**Синтаксис:** Obsidian / LaTeX

---

## 1. Предмет исследования

В классической линейной алгебре операции над матрицами **фиксированной размерности** определены строго. Однако во многих прикладных задачах возникают **матрицы разной размерности**, которые необходимо **сопрягать**:

- в радиолокации — срезы трёхмерного куба данных,
- в нейросетях — проекции входов, ключей и значений,
- в генетике — рекомбинации хромосом,
- в кристаллографии — переходы между решётками,
- в transfer learning — перенос признаков между доменами.

Во всех этих задачах **неявно используется интерфейс** — оператор, который переводит одну матрицу в другую. Но **единой теории интерфейсов** нет.

**Предмет исследования:** композиция матриц разной размерности через интерфейс.

**Цель:** построить метод MCM (Matrix Composition Method), описывающий композицию матриц разной размерности, её законы, свойства и применения.

---

### 1.1. Постановка задачи

Даны две матрицы разной размерности:

$$
A \in \mathbb{R}^{m \times n}, \quad B \in \mathbb{R}^{p \times q}.
$$

**Классический ответ:** операции над ними запрещены.

**Наш ответ:** операция возможна через **интерфейс**.

Мы вводим **левый оператор** $U$ и **правый оператор** $V$:

$$
B = U A V^T,
$$

где:

- $A \in \mathbb{R}^{m \times n}$ — **ядро** (содержание),
- $U \in \mathbb{R}^{p \times m}$ — **левый оператор** (структура строк),
- $V \in \mathbb{R}^{q \times n}$ — **правый оператор** (структура столбцов),
- $B \in \mathbb{R}^{p \times q}$ — **образ** (результат).

**Условия:**

$$
U^T U = I_m, \quad V^T V = I_n.
$$

**Операторы полуортогональны.** Это обеспечивает **сохранение субстанции**:

$$
\text{Tr}(B^T B) = \text{Tr}(A^T A).
$$

---

### 1.2. Троичная природа

MCM-преобразование имеет **троичную природу**:

| Сущность | Роль | Что даёт |
|----------|------|----------|
| $U$ | **Отец** | Структуру строк |
| $V$ | **Мать** | Структуру столбцов |
| $A$ | **Содержание** | Субстанцию |
| $B$ | **Ребёнок** | Результат |

**Ни одна из трёх сущностей не является главной.**
**Все три необходимы и равноправны.**

**Следствие:**

- Если $U = 0$ — $B = 0$,
- Если $V = 0$ — $B = 0$,
- Если $A = 0$ — $B = 0$.

**Структура так же важна, как содержание.**

---

### 1.3. Множественность как фича

Без фиксации $U$ или $V$ MCM-преобразование имеет **бесконечно много решений**.

**Это не баг, а фича.**

**Множественность — это:**

1. **Пространство возможностей** — выбор из бесконечного множества,
2. **Источник мутаций** — вариации интерфейса,
3. **Адаптация** — подстройка под задачу.

**Это заимствовано у природы:**

- Один генотип → много фенотипов,
- Одна ДНК → много белков,
- Один сигнал → много интерпретаций.

**Фиксация $U$ или $V$** сужает пространство до **единственного** решения. Это — **теорема о необходимости опоры**.
## 1.4. Две задачи: взаимодействие и сравнение

MCM описывает **две фундаментальные задачи**, которые часто путают.

### Задача 1: Взаимодействие

> **Как матрица $A$ может передать своё содержание матрице $B$?**

**Формула:**

$$
B = U A V^T
$$

**Смысл:** $A$ **преобразуется** в $B$ через интерфейс $U, V^T$.

**Что важно:**

- **Сохранение субстанции** — $\text{Tr}(B^T B) = \text{Tr}(A^T A)$,
- **Опора** — фиксация $U$ или $V$ для единственности,
- **Ранг** — $\text{rank}(B) \le \text{rank}(A)$,
- **Композиция** — цепочки интерфейсов.

**Пример:** «Чиполлино» — три среза передают содержание в когерентное ядро.

### Задача 2: Сравнение

> **Насколько матрица $A$ похожа на матрицу $B$?**

**Формула:**

$$
D(A, B) = \min_{U, V} \|B - U A V^T\|_F
$$

**Смысл:** ищем **оптимальный интерфейс**, который **минимизирует различие** между $A$ и $B$.

**Что важно:**

- **Метрика** — $D(A, B)$ — мера различия,
- **Опора** — фиксация $U$ или $V$,
- **Класс эквивалентности** — $[A] = \{U A V^T\}$.

**Пример:** сравнение двух радаров, двух нейросетей, двух сигналов.

### Связь задач

**Взаимодействие** — это **частный случай** сравнения, когда $D = 0$:

$$
D(A, B) = 0 \iff B = U A V^T \text{ для некоторых } U, V.
$$

**Сравнение** — это **обобщение** взаимодействия, когда $D > 0$:

$$
D(A, B) > 0 \iff B \ne U A V^T \text{ для всех } U, V.
$$

**MCM описывает оба случая.**

---

## 1.5. Цель статьи

1. **Формализовать MCM** — определение, свойства, теоремы.
2. **Показать троичную природу** — $U, V, A$ равноправны.
3. **Доказать теорему о ранге** — $\text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V))$.
4. **Сформулировать теорему о необходимости опоры** — множественность как фича.
5. **Ввести принцип минимальности** — размерности $U$ и $V$.
6. **Доказать закон сохранения субстанции** — $\text{Tr}(B^T B) = \text{Tr}(A^T A)$.
7. **Показать взаимодействие** — прямая задача.
8. **Показать сравнение** — обратная задача.
9. **Показать композицию MCM** — наследование интерфейсов.
10. **Показать мутации MCM** — где и как.
11. **Показать применение** — «Чиполлино», Transformer.
12. **Построить метрику MCM** — как сравнивать.

## 1.6. Обзор существующих подходов

MCM **не возникает на пустом месте**. Он **обобщает** и **дополняет** существующие методы. Ниже — краткий обзор.

---

### 1.6.1. Задача Проcrustes (Orthogonal Procrustes)

**Формулировка:**

$$
\arg \min_R \|A R - B\|_F, \quad R^T R = I.
$$

**Что это:** поиск **одной** ортогональной матрицы $R$, которая **поворачивает** $A$ так, чтобы она **совпала** с $B$.

**Где применяется:**

- компьютерное зрение — 3D-выравнивание объектов,
- биоинформатика — сравнение формы белков,
- ReBasin — слияние нейросетей,
- астрономия — выравнивание звёздных карт.

**Кто занимался:**

- Schönemann $1966$ — классическая работа,
- Kabsch $1976$ — алгоритм для 3D,
- Umeyama $1991$ — с масштабом и сдвигом,
- Gower & Dijksterhuis $2004$ — обобщённая задача.

**Ограничение:** $A$ и $B$ **одной размерности**. $R$ — **квадратная**.

**MCM:** обобщает Проcrustes на случай **разной размерности** и **двух операторов** $$U, V$$.

---

### 1.6.2. Subspace Alignment & Domain Adaptation

**Формулировка:**

$$
A_{\text{проекция}} = P_L B P_R,
$$

где $P_L, P_R$ — прямоугольные проекторы.

**Что это:** выравнивание подпространств для переноса обучения.

**Где применяется:**

- transfer learning — перенос признаков между доменами,
- domain adaptation — адаптация к новым данным,
- subspace clustering — кластеризация подпространств.

**Кто занимался:**

- Fernando et al. $ICCV, 2013$,
- Chen et al. $2015$,
- Gopalan et al. $2011$.

**Ограничение:** проекторы $P_L, P_R$ ищутся **одновременно**. Нет **фиксации опоры**.

**MCM:** добавляет **опору** и **троичную природу**.

---

### 1.6.3. Generalized Canonical Correlation Analysis $GCCA$

**Формулировка:**

$$
C_A = A^T A, \quad C_B = B^T B, \quad T = C_B C_A^{-1}.
$$

**Что это:** приведение разнородных матриц к общей ковариационной плоскости.

**Где применяется:**

- нейровизуализация (fMRI),
- многоканальные датчики,
- анализ данных,
- kernel methods.

**Кто занимался:**

- Hotelling $1936$ — классическая CCA,
- Hardoon et al. $2004$ — CCA в машинном обучении,
- Bach & Jordan $2005$ — kernel CCA,
- Tenenhaus & Tenenhaus $2011$ — RGCCA.

**Ограничение:** **не сохраняет субстанцию**. Ищет **общие направления**, а не **передачу**.

**MCM:** сохраняет субстанцию через $\text{Tr}(B^T B) = \text{Tr}(A^T A)$.

---

### 1.6.4. Riemannian Manifolds & Fréchet Mean

**Формулировка:**

Сравнение матриц как точек на **искривлённом многообразии** положительно определённых матриц.

$$
d(A, B) = \| \log(A^{-1/2} B A^{-1/2}) \|_F.
$$

**Что это:** метрика на многообразии ковариационных матриц.

**Где применяется:**

- representational similarity analysis,
- сравнение нейросетевых представлений,
- обработка сигналов,
- computer vision.

**Кто занимался:**

- Pennec et al. $2006$,
- Fletcher & Joshi $2007$,
- Barachant et al. $2012$,
- Congedo et al. $2015$.

**Ограничение:** работает с **ковариационными матрицами** одинаковой размерности.

**MCM:** работает с **произвольными матрицами разной размерности**.

---

### 1.6.5. Transfer Learning & Domain Adaptation

**Формулировка:**

$$
f_B = f_A \circ \phi.
$$

**Что это:** перенос знаний из одной области в другую.

**Где применяется:**

- нейросети,
- доменная адаптация,
- few-shot learning,
- meta-learning.

**Кто занимался:**

- Pan & Yang $2010$ — обзор,
- Zhuang et al. $2021$ — обзор,
- Wang & Deng $2018$ — domain adaptation.

**Ограничение:** **не формализовано** как матричное преобразование.

**MCM:** формализует через $B = U A V^T$.

---

### 1.6.6. Operator-Valued Kernels

**Формулировка:**

$$
K: X \times X \to \mathcal{L}(Y).
$$

**Что это:** ядра, принимающие значения в пространстве операторов.

**Где применяется:**

- машинное обучение,
- функциональный анализ,
- structured prediction.

**Кто занимался:**

- Micchelli & Pontil $2005$,
- Caponnetto et al. $2008$,
- Álvarez et al. $2012$.

**Ограничение:** **слишком общее**. Не даёт конкретных теорем.

**MCM:** даёт конкретные теоремы (о ранге, об опоре, о сохранении).

---

### 1.6.7. Сводная таблица

| Подход | Матриц | Размерности | Операторов | Опора | Сохранение субстанции | Композиция |
|--------|--------|-------------|------------|-------|----------------------|------------|
| **Проcrustes** | 2 | Совпадают | 1 | Нет | Следствие | Нет |
| **Subspace Alignment** | 2 | Разные | 2 | Нет | Нет | Нет |
| **GCCA** | 2+ | Разные | Ковариации | Нет | Нет | Нет |
| **Riemannian** | 2+ | Совпадают | Метрика | Нет | Нет | Нет |
| **Transfer Learning** | 2 | Разные | Функция | Нет | Нет | Да |
| **Operator Kernels** | 2+ | Разные | Ядро | Нет | Нет | Да |
| **MCM** | 2 | **Разные** | **2** | **Есть** | **Есть** | **Есть** |

**MCM** — **обобщает** и **дополняет** все перечисленные подходы.

---

### 1.6.8. Что нового в MCM

1. **Троичная природа** — $U, V, A$ равноправны.
2. **Теорема о необходимости опоры** — множественность как фича.
3. **Теорема о ранге** — $\text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V))$.
4. **Принцип минимальности** — размерности $U$ и $V$.
5. **Закон сохранения субстанции** — $\text{Tr}(B^T B) = \text{Tr}(A^T A)$.
6. **Композиция MCM** — наследование интерфейсов.
7. **Мутации MCM** — где и как.

**MCM — не вместо. MCM — над.**
## 2. Определение MCM

### 2.1. Основное определение

> **Определение 2.1 (MCM-преобразование).**
> Пусть даны матрицы:
> $$
> A \in \mathbb{R}^{m \times n}, \quad U \in \mathbb{R}^{p \times m}, \quad V \in \mathbb{R}^{q \times n}.
> $$
> **MCM-преобразованием** называется операция:
> $$
> B = U A V^T,
> $$
> где:
> - $A$ — **ядро** (содержание),
> - $U$ — **левый оператор** (структура строк),
> - $V$ — **правый оператор** (структура столбцов),
> - $B \in \mathbb{R}^{p \times q}$ — **образ** (результат).

**Размерности:**

$$
\underbrace{(p \times m)}_{U} \cdot \underbrace{(m \times n)}_{A} \cdot \underbrace{(n \times q)}_{V^T} = \underbrace{(p \times q)}_{B}.
$$

**Контроль размерностей:**

| Матрица | Размерность | Роль |
|---------|-------------|------|
| $A$ | $m \times n$ | Ядро |
| $U$ | $p \times m$ | Левый оператор |
| $V$ | $q \times n$ | Правый оператор |
| $B$ | $p \times q$ | Образ |

---

### 2.2. Условия полуортогональности

> **Определение 2.2 (Условие MCM).**
> Операторы $U$ и $V$ называются **допустимыми**, если:
> $$
> U^T U = I_m, \quad V^T V = I_n.
> $$

**Смысл:**

- $U$ — **полуортогональная** матрица: её столбцы ортонормированы,
- $V$ — **полуортогональная** матрица: её столбцы ортонормированы.

**Геометрически:**

- $U$ **вкладывает** $m$-мерное пространство в $p$-мерное,
- $V$ **вкладывает** $n$-мерное пространство в $q$-мерное.

**Если $p = m$** — $U$ **квадратная** ортогональная.
**Если $p > m$** — $U$ **расширяет** пространство.
**Если $p < m$** — $U$ **сжимает** пространство.

**То же для $V$.**

---

### 2.3. Сохранение субстанции

> **Теорема 2.1 (Сохранение субстанции).**
> Если $U^T U = I_m$ и $V^T V = I_n$, то:
> $$
> \text{Tr}(B^T B) = \text{Tr}(A^T A).
> $$

**Доказательство:**

$$
\text{Tr}(B^T B) = \text{Tr}((U A V^T)^T (U A V^T)) = \text{Tr}(V A^T U^T U A V^T).
$$

Поскольку $U^T U = I_m$:

$$
= \text{Tr}(V A^T A V^T) = \text{Tr}(A^T A V^T V).
$$

Поскольку $V^T V = I_n$:

$$
= \text{Tr}(A^T A). \quad \blacksquare
$$

**Смысл:** MCM **сохраняет субстанцию**. Если $A$ содержит 1 литр, $B$ содержит **тот же** 1 литр — но в другой форме.

---

### 2.4. Троичная природа

> **Определение 2.3 (Троичная природа MCM).**
> MCM-преобразование имеет **три равноправные сущности**:
> 1. **Левый оператор** $U$ — структура строк,
> 2. **Правый оператор** $V$ — структура столбцов,
> 3. **Ядро** $A$ — содержание.

**Смысл:**

- **Ни одна** из трёх сущностей **не является главной**,
- **Все три** необходимы и равноправны,
- **Структура** так же важна, как **содержание**.

**Следствие:**

- Если $U = 0$ — $B = 0$,
- Если $V = 0$ — $B = 0$,
- Если $A = 0$ — $B = 0$.

---

### 2.5. Отличие от SVD

**SVD:**

$$
X = U \Sigma V^T.
$$

- $U, V$ — **квадратные** ортогональные,
- $\Sigma$ — **диагональная** с сингулярными числами,
- **самопроекция** одной матрицы.

**MCM:**

$$
B = U A V^T.
$$

- $U, V$ — **полуортогональные**,
- $A$ — **произвольная** матрица,
- **взаимопроекция** между двумя матрицами.

**Ключевое отличие:**

| Свойство | SVD | MCM |
|----------|-----|-----|
| Что делает | Разлагает одну матрицу | Преобразует $A$ в $B$ |
| Компоненты | $U, \Sigma, V^T$ | $U, A, V^T$ |
| Размерности | Квадратные $U, V$ | Полуортогональные $U, V$ |
| Смысл | Самопроекция | Взаимопроекция |
| $\Sigma$ | Масштаб | Заменён на $A$ |

**SVD — частный случай MCM**, когда:

- $A = \Sigma$ — диагональная,
- $U, V$ — квадратные,
- $B = X$.

---

### 2.6. Отличие от Проcrustes

**Проcrustes:**

$$
\arg \min_R \|A R - B\|_F, \quad R^T R = I.
$$

- **Один** оператор $R$,
- $A$ и $B$ **одной размерности**,
- $R$ — **квадратная**.

**MCM:**

$$
B = U A V^T.
$$

- **Два** оператора $U, V$,
- $A$ и $B$ **разной размерности**,
- $U, V$ — **полуортогональные**.

**Ключевое отличие:**

| Свойство | Проcrustes | MCM |
|----------|------------|-----|
| Операторов | 1 | 2 |
| Размерности | Совпадают | Разные |
| Опора | Нет | Есть |
| Сохранение субстанции | Следствие | Закон |
| Композиция | Нет | Есть |

**Проcrustes — частный случай MCM**, когда:

- $U = I$ (или $V = I$),
- $A$ и $B$ одной размерности,
- опора не нужна.

---

### 2.7. Пример: «Чиполлино»

В радиолокационной системе «Чиполлино»:

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**MCM-интерпретация:**

| Матрица | Роль | Размерность |
|---------|------|-------------|
| $\widehat{\mathbf{X}}_{AB}$ | Левый оператор $U$ | $1024 \times 128$ |
| $\widehat{\mathbf{X}}_{DV}^T$ | Ядро $A$ | $128 \times 14500$ |
| $\mathbf{X}_{DA}$ | Правый оператор $V$ | $14500 \times 1024$ |
| $\widehat{\mathbf{X}}_{AA}$ | Образ $B$ | $1024 \times 1024$ |

**Контроль размерностей:**

$$
(1024 \times 128) \cdot (128 \times 14500) \cdot (14500 \times 1024) = 1024 \times 1024.
$$

**Смысл:** три среза **композируются** в когерентное ядро.

---

### 2.8. Пример: Transformer

В механизме самовнимания Transformer:

$$
Q = X W_Q, \quad K = X W_K, \quad V = X W_V.
$$

**MCM-интерпретация:**

| Матрица | Роль | Размерность |
|---------|------|-------------|
| $X$ | Ядро $A$ | $n \times 768$ |
| $W_Q$ | Правый оператор $V$ | $768 \times 64$ |
| $Q$ | Образ $B$ | $n \times 64$ |

**MCM-формула:**

$$
Q = X W_Q.
$$

**Смысл:** вход $X$ **проецируется** в пространство запросов $Q$.

**Аналогично для $K$ и $V$.**

---

### 2.9. Сводка определений

| Понятие | Обозначение | Смысл |
|---------|-------------|-------|
| MCM-преобразование | $B = U A V^T$ | Композиция |
| Ядро | $A$ | Содержание |
| Левый оператор | $U$ | Структура строк |
| Правый оператор | $V$ | Структура столбцов |
| Образ | $B$ | Результат |
| Условие | $U^T U = I_m$, $V^T V = I_n$ | Полуортогональность |
| Закон | $\text{Tr}(B^T B) = \text{Tr}(A^T A)$ | Сохранение субстанции |
| Троичность | $U, V, A$ | Равноправие |
## 3. Троичная природа MCM 
### 3.1. Три сущности

MCM-преобразование:

$$
B = U A V^T
$$

содержит **три сущности**, каждая из которых **равноправна**:

| Сущность | Обозначение | Роль | Что даёт |
|----------|-------------|------|----------|
| **Левый оператор** | $U$ | Структура строк | Пространство наблюдений |
| **Ядро** | $A$ | Содержание | Субстанцию |
| **Правый оператор** | $V$ | Структура столбцов | Пространство признаков |

**Ключевой тезис:**

> **Ни одна из трёх сущностей не является главной.**
> **Все три необходимы и равноправны.**

---

### 3.2. Почему троичная, а не бинарная

В классической линейной алгебре обычно рассматривают **два** объекта:

- $A$ — матрица,
- $B$ — матрица.

И **один** оператор между ними:

- $R$ — матрица перехода.

**MCM** вводит **третий** объект:

- $U$ — левый оператор,
- $A$ — ядро,
- $V$ — правый оператор.

**Почему это важно:**

1. **Структура строк** и **структура столбцов** — **разные** вещи. Они не сводятся к одной матрице $R$.
2. **Содержание** $A$ — **отдельная** сущность. Оно не принадлежит ни $U$, ни $V$.
3. **Равноправие** — все три влияют на результат **одинаково**.

---

### 3.3. Геометрическая интерпретация

**Левый оператор $U$:**

$$
U: \mathbb{R}^m \to \mathbb{R}^p.
$$

- **Вкладывает** $m$-мерное пространство в $p$-мерное,
- **Определяет**, как строки $A$ отображаются в строки $B$,
- **Задаёт** пространство наблюдений.

**Ядро $A$:**

$$
A: \mathbb{R}^n \to \mathbb{R}^m.
$$

- **Преобразует** $n$-мерное пространство в $m$-мерное,
- **Содержит** субстанцию,
- **Определяет** содержание.

**Правый оператор $V$:**

$$
V: \mathbb{R}^n \to \mathbb{R}^q.
$$

- **Вкладывает** $n$-мерное пространство в $q$-мерное,
- **Определяет**, как столбцы $A$ отображаются в столбцы $B$,
- **Задаёт** пространство признаков.

**Композиция:**

$$
B = U A V^T: \mathbb{R}^q \to \mathbb{R}^p.
$$

---

### 3.4. Аналогия с «отцом, матерью и содержанием»

Троичная природа MCM имеет **естественную аналогию**:

| MCM | Аналогия | Роль |
|-----|----------|------|
| $U$ | **Отец** | Даёт структуру строк |
| $V$ | **Мать** | Даёт структуру столбцов |
| $A$ | **Содержание** | Даёт субстанцию |
| $B$ | **Ребёнок** | Результат |

**Смысл:**

- **Отец** и **мать** дают **структуру**,
- **Содержание** — **третья** сущность,
- **Ребёнок** — **результат** композиции.

**Следствие:**

- Без отца — нет ребёнка,
- Без матери — нет ребёнка,
- Без содержания — нет ребёнка.

**Все три обязательны.**

---

### 3.5. Теорема о равноправии

> **Теорема 3.1 (Равноправие сущностей).**
> В MCM-преобразовании $B = U A V^T$ **все три сущности** ($U$, $A$, $V$) **равноправны**:
> 1. Изменение $U$ **меняет** $B$,
> 2. Изменение $A$ **меняет** $B$,
> 3. Изменение $V$ **меняет** $B$.
>
> **Ни одна из сущностей не является «главной».**

**Доказательство:**

Пусть $U' \ne U$. Тогда:

$$
B' = U' A V^T \ne U A V^T = B
$$

(в общем случае). Аналогично для $A$ и $V$. $\blacksquare$

**Следствие:**

- **Структура** так же важна, как **содержание**,
- **Нельзя** пренебречь ни $U$, ни $V$, ни $A$,
- **Все три** влияют на результат.

---

### 3.6. Следствие: структура важнее содержания?

**В классической линейной алгебре** обычно считают:

> $A$ — **главное**. $U, V$ — **вспомогательные**.

**В MCM:**

> $U, V, A$ — **равноправны**.

**Но есть нюанс:**

Если $A$ **фиксирована** (дана), то $B$ **определяется** выбором $U$ и $V$. То есть **структура** определяет, **как** содержание будет работать.

**Пример в радиолокации:**

- Помеха РЭБ имеет **огромную энергию** ($\Sigma$ большое),
- Но её **структура** хаотична (высокий ранг),
- Цель имеет **малую энергию** ($\Sigma$ малое),
- Но её **структура** когерентна (низкий ранг).

**Система ловит не энергию, а структуру.**

**Пример в Transformer:**

- Веса $W_Q, W_K, W_V$ **обучаются**,
- Но **архитектура** (структура) **фиксирована**,
- Именно структура определяет, **как** веса будут работать.

**Структура первична. Содержание вторично.**

---

### 3.7. Троичная природа и ранг

> **Теорема 3.2 (Ранг троичной композиции).**
> $$
> \text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V)).
> $$

**Следствие:**

- Если $U$ низкого ранга — $B$ низкого ранга,
- Если $A$ низкого ранга — $B$ низкого ранга,
- Если $V$ низкого ранга — $B$ низкого ранга.

**Все три сущности ограничивают ранг $B$.**

**Структура и содержание равноправны — и в ранге тоже.**

---

### 3.8. Троичная природа и сохранение субстанции

> **Теорема 3.3 (Сохранение субстанции).**
> Если $U^T U = I_m$ и $V^T V = I_n$, то:
> $$
> \text{Tr}(B^T B) = \text{Tr}(A^T A).
> $$

**Следствие:**

- **Субстанция** сохраняется при MCM,
- **Форма** меняется,
- **Содержание** перераспределяется.

**Это — не свойство $U$, $V$ или $A$ по отдельности.**
**Это — свойство композиции.**

---

### 3.9. Троичная природа и мутации

**Мутация** может произойти в **любой** из трёх сущностей:

| Мутация в | Что меняется | Что сохраняется |
|-----------|--------------|-----------------|
| $U$ | Структура строк | Структура столбцов, содержание |
| $V$ | Структура столбцов | Структура строк, содержание |
| $A$ | Содержание | Структура строк и столбцов |

**Все три мутации наследуются.**

**Это — основа для диагностики и прогноза.**

---

### 3.10. Пример: «Чиполлино»

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**Троичная природа:**

| Сущность | Роль | Что даёт |
|----------|------|----------|
| $\widehat{\mathbf{X}}_{AB}$ | Левый оператор $U$ | Структура антенн → дальность |
| $\widehat{\mathbf{X}}_{DV}^T$ | Ядро $A$ | Содержание |
| $\mathbf{X}_{DA}$ | Правый оператор $V$ | Структура дальность → антенны |
| $\widehat{\mathbf{X}}_{AA}$ | Образ $B$ | Когерентное ядро |

**Смысл:** три среза **равноправно** участвуют в композиции.

---

### 3.11. Пример: Transformer

$$
Q = X W_Q, \quad K = X W_K, \quad V = X W_V.
$$

**Троичная природа для $Q$:**

| Сущность | Роль | Что даёт |
|----------|------|----------|
| $X$ | Ядро $A$ | Содержание (вход) |
| $W_Q$ | Правый оператор $V$ | Структура (проекция) |
| $Q$ | Образ $B$ | Запрос |

**Смысл:** вход $X$ **проецируется** в запрос $Q$ через $W_Q$.

**Аналогично для $K$ и $V$.**

---

### 3.12. Сводка

| Свойство | MCM |
|----------|-----|
| Сущностей | **Три** ($U, A, V$) |
| Равноправие | **Да** |
| Структура vs содержание | **Равноправны** |
| Ранг | $\le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V))$ |
| Сохранение субстанции | $\text{Tr}(B^T B) = \text{Tr}(A^T A)$ |
| Мутации | В $U$, $V$ или $A$ |

## 4. Теорема о ранге 
### 4.1. Формулировка

> **Теорема 4.1 (Ранг MCM-композиции).**
> Пусть:
> $$
> B = U A V^T,
> $$
> где $A \in \mathbb{R}^{m \times n}$, $U \in \mathbb{R}^{p \times m}$, $V \in \mathbb{R}^{q \times n}$.
> Тогда:
> $$
> \text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V)).
> $$

**Смысл:**

- Ранг $B$ **не может превышать** ранг **любой** из трёх сущностей,
- **Все три** сущности **ограничивают** ранг результата,
- **Структура** и **содержание** равноправны в ограничении.

---

### 4.2. Доказательство

**Шаг 1.** Рассмотрим произведение $U A$:

$$
U A \in \mathbb{R}^{p \times n}.
$$

**Известно:**

$$
\text{rank}(U A) \le \min(\text{rank}(U), \text{rank}(A)).
$$

**Шаг 2.** Рассмотрим произведение $(U A) V^T$:

$$
B = (U A) V^T \in \mathbb{R}^{p \times q}.
$$

**Известно:**

$$
\text{rank}(B) \le \min(\text{rank}(U A), \text{rank}(V)).
$$

**Шаг 3.** Подставляем:

$$
\text{rank}(B) \le \min(\text{rank}(U A), \text{rank}(V)) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V)).
$$

**Теорема доказана.** $\blacksquare$

---

### 4.3. Следствия

> **Следствие 4.1 (Необходимое условие).**
> Если $\text{rank}(B) = r_B$, то:
> $$
> \text{rank}(U) \ge r_B, \quad \text{rank}(A) \ge r_B, \quad \text{rank}(V) \ge r_B.
> $$

**Смысл:** чтобы получить $B$ ранга $r_B$, **все три** сущности должны иметь ранг **не меньше** $r_B$.

> **Следствие 4.2 (Достаточное условие).**
> Если $\text{rank}(U) \ge r_B$, $\text{rank}(A) \ge r_B$, $\text{rank}(V) \ge r_B$ и $U, A, V$ **согласованы**, то **может** быть $\text{rank}(B) = r_B$.

**Смысл:** необходимое условие **не является достаточным**. Нужна ещё **согласованность**.

> **Следствие 4.3 (Ограничение размерности).**
> $$
> \text{rank}(B) \le \min(p, q, m, n).
> $$

**Смысл:** ранг $B$ ограничен **не только** рангами $U, A, V$, но и **размерностями** пространств.

---

### 4.4. Пример: «Чиполлино»

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**Размерности:**

| Матрица | Размерность | Ранг |
|---------|-------------|------|
| $\widehat{\mathbf{X}}_{AB}$ | $1024 \times 128$ | $\le 128$ |
| $\widehat{\mathbf{X}}_{DV}^T$ | $128 \times 14500$ | $\le 128$ |
| $\mathbf{X}_{DA}$ | $14500 \times 1024$ | $\le 1024$ |
| $\widehat{\mathbf{X}}_{AA}$ | $1024 \times 1024$ | $\le 128$ |

**По теореме 4.1:**

$$
\text{rank}(\widehat{\mathbf{X}}_{AA}) \le \min(128, 128, 1024) = 128.
$$

**Смысл:**

- Когерентное ядро **имеет ранг $\le 128$**,
- Это **фундаментальное свойство** «Чиполлино»,
- Именно оно позволяет **отсекать шум** и **фокусировать сигнал**.

---

### 4.5. Пример: Transformer

$$
Q = X W_Q.
$$

**Размерности:**

| Матрица | Размерность | Ранг |
|---------|-------------|------|
| $X$ | $n \times 768$ | $\le \min(n, 768)$ |
| $W_Q$ | $768 \times 64$ | $\le 64$ |
| $Q$ | $n \times 64$ | $\le 64$ |

**По теореме 4.1:**

$$
\text{rank}(Q) \le \min(\text{rank}(X), \text{rank}(W_Q)) \le 64.
$$

**Смысл:**

- Запрос $Q$ **имеет ранг $\le 64$**,
- Это **ограничение архитектуры**,
- Оно **осознанное** — $d_k = 64$ в стандартном Transformer.

---

### 4.6. Ранг и мутации

**Мутация в $U$:**

$$
B' = U' A V^T.
$$

**Ранг:**

$$
\text{rank}(B') \le \min(\text{rank}(U'), \text{rank}(A), \text{rank}(V)).
$$

**Если $\text{rank}(U') < \text{rank}(U)$** — ранг $B'$ **может упасть**.

**Мутация в $A$:**

$$
B' = U A' V^T.
$$

**Ранг:**

$$
\text{rank}(B') \le \min(\text{rank}(U), \text{rank}(A'), \text{rank}(V)).
$$

**Если $\text{rank}(A') < \text{rank}(A)$** — ранг $B'$ **может упасть**.

**Мутация в $V$:**

$$
B' = U A V'^T.
$$

**Ранг:**

$$
\text{rank}(B') \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V')).
$$

**Если $\text{rank}(V') < \text{rank}(V)$** — ранг $B'$ **может упасть**.

**Вывод:** мутация в **любой** из трёх сущностей **может уменьшить** ранг $B$.

---

### 4.7. Ранг и композиция

**Композиция MCM:**

$$
B = U_1 A V_1^T, \quad C = U_2 B V_2^T.
$$

**Тогда:**

$$
C = (U_2 U_1) A (V_2 V_1)^T.
$$

**Ранг:**

$$
\text{rank}(C) \le \min(\text{rank}(U_2 U_1), \text{rank}(A), \text{rank}(V_2 V_1)).
$$

**Известно:**

$$
\text{rank}(U_2 U_1) \le \min(\text{rank}(U_2), \text{rank}(U_1)),
$$
$$
\text{rank}(V_2 V_1) \le \min(\text{rank}(V_2), \text{rank}(V_1)).
$$

**Следствие:** композиция **не увеличивает** ранг. Она **может только уменьшить** его.

---

### 4.8. Ранг и сохранение субстанции

**Теорема 4.1** и **закон сохранения субстанции** — **разные** вещи:

| Свойство | Что сохраняется |
|----------|-----------------|
| Сохранение субстанции | $\text{Tr}(B^T B) = \text{Tr}(A^T A)$ |
| Теорема о ранге | $\text{rank}(B) \le \text{rank}(A)$ |

**Субстанция** сохраняется **точно**.
**Ранг** — **может уменьшиться**.

**Это — важно.** Потому что:

- **Энергия** сохраняется,
- **Структура** может упроститься,
- **Информация** может сжаться.

---

### 4.9. Практическое значение

**Теорема о ранге** имеет **практическое значение**:

1. **Фильтрация шума.** Если шум имеет **высокий ранг**, а сигнал — **низкий**, MCM **отсекает шум**.
2. **Сжатие данных.** Если $B$ имеет **низкий ранг**, его можно **хранить компактно**.
3. **Диагностика.** Если ранг $B$ **упал** — значит, мутация в $U$, $A$ или $V$.
4. **Проектирование.** Если нужен $B$ ранга $r_B$, нужно обеспечить $\text{rank}(U), \text{rank}(A), \text{rank}(V) \ge r_B$.

---

### 4.10. Сводка

| Свойство | Формула |
|----------|---------|
| Ранг $B$ | $\text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V))$ |
| Необходимое условие | $\text{rank}(U), \text{rank}(A), \text{rank}(V) \ge r_B$ |
| Ограничение размерности | $\text{rank}(B) \le \min(p, q, m, n)$ |
| Мутация | Может уменьшить ранг |
| Композиция | Не увеличивает ранг |

## 5. Теорема о необходимости опоры
### 5.1. Постановка

MCM-преобразование:

$$
B = U A V^T.
$$

**Дано:** $A$, $B$.
**Ищем:** $U$, $V$.

**Вопрос:** сколько решений имеет эта задача?

**Ответ:** **бесконечно много**, если $U$ и $V$ не зафиксированы.

**Это — не баг. Это — фича.**

---

### 5.2. Множественность решений

> **Теорема 5.1 (Множественность).**
> Если $U$ и $V$ свободны, то MCM-преобразование $B = U A V^T$ имеет **бесконечно много** решений.

**Доказательство:**

Пространство допустимых $U$ — многообразие Штифеля $V_m(\mathbb{R}^p)$, размерность:

$$
\dim V_m(\mathbb{R}^p) = pm - \frac{m(m+1)}{2}.
$$

Пространство допустимых $V$ — многообразие Штифеля $V_n(\mathbb{R}^q)$, размерность:

$$
\dim V_n(\mathbb{R}^q) = qn - \frac{n(n+1)}{2}.
$$

**Общая размерность пространства решений:**

$$
\dim = \left( pm - \frac{m(m+1)}{2} \right) + \left( qn - \frac{n(n+1)}{2} \right).
$$

**Это — огромное пространство.** Оно **не пусто** и **не конечно**. $\blacksquare$

---

### 5.3. Множественность как фича

**Классическая линейная алгебра:**

> Если решение не единственно — задача **некорректна**.

**MCM:**

> Множественность — это **пространство возможностей**.
> Это **плюс**.
> Это **возможность мутации**.

**Это заимствовано у природы:**

| Природа | MCM |
|---------|-----|
| Один генотип → много фенотипов | Одна $A$ → много $B$ |
| Одна ДНК → много белков | Один интерфейс → много результатов |
| Один сигнал → много интерпретаций | Одна структура → много содержаний |

**Множественность — это **источник адаптации** и **эволюции**.

---

### 5.4. Теорема о необходимости опоры

> **Теорема 5.2 (Необходимость опоры).**
> MCM-преобразование $B = U A V^T$ имеет **единственное** решение тогда и только тогда, когда **хотя бы один** из операторов ($U$ или $V$) **фиксирован**.

**Доказательство:**

**Необходимость.** Если $U$ и $V$ свободны, то по теореме 5.1 решений бесконечно много. Значит, для единственности нужно зафиксировать хотя бы один.

**Достаточность.** Пусть $U$ фиксирован. Тогда:

$$
B = U A V^T \implies U^T B = A V^T \implies V^T = A^+ U^T B,
$$

где $A^+$ — псевдообратная матрица. Если $A$ полного ранга и $V^T V = I_n$, то $V$ **единственна** (с точностью до знаков).

Аналогично для фиксированного $V$. $\blacksquare$

---

### 5.5. Три режима MCM

| Режим | Что фиксировано | Что ищется | Единственность |
|-------|-----------------|------------|----------------|
| **1** | $U, V, A$ | $B$ | Да, всегда |
| **2** | $A, B, U$ | $V$ | Да, если $A$ полного ранга |
| **3** | $A, B, V$ | $U$ | Да, если $A$ полного ранга |
| **4** | $A, B$ | $U, V$ | Нет, бесконечно много |

**Режим 1** — **прямая задача**.
**Режим 2, 3** — **обратные задачи с опорой**.
**Режим 4** — **обратная задача без опоры**.

---

### 5.6. Выбор опоры

**Вопрос:** какую матрицу фиксировать — $U$ или $V$?

**Ответ:** зависит от задачи.

**Если $U$ фиксирован:**

- $U$ — **структура строк**,
- $V$ — **ищется**,
- **Пример:** в «Чиполлино» антенная решётка фиксирована физикой.

**Если $V$ фиксирован:**

- $V$ — **структура столбцов**,
- $U$ — **ищется**,
- **Пример:** в доплеровской обработке модель движения фиксирована сценарием.

**Если оба фиксированы:**

- $B$ **вычисляется** напрямую,
- **Пример:** в Transformer $W_Q, W_K, W_V$ обучены.

---

### 5.7. Принцип минимальности

**Ты говоришь:**

> Размерности $U$ и $V$ **не должны быть больше**, чем нужно для перевода $A$ в $B$.

**Формально:**

$$
U \in \mathbb{R}^{p \times m}, \quad V \in \mathbb{R}^{q \times n},
$$

где:

- $p$ — число строк $B$,
- $q$ — число столбцов $B$,
- $m$ — число строк $A$,
- $n$ — число столбцов $A$.

**Если $p > m$** — $U$ **расширяет** пространство.
**Если $p < m$** — $U$ **сжимает** пространство.

**Требование:** $U$ и $V$ **минимальны** по размерности.

**Зачем:**

1. **Скорость вычислений** — меньше размерность → быстрее,
2. **Память** — меньше размерность → меньше памяти,
3. **Интерпретируемость** — меньше размерность → проще понять.

---

### 5.8. След и ранг $A$

**Ты говоришь:**

> След матрицы $A$ и её ранг **от нас не зависят**.

**Согласен.** $A$ — **дана**. Мы **не можем** её изменить.

**Но:**

- $\text{Tr}(A^T A)$ — **сохраняется** при MCM,
- $\text{rank}(A)$ — **ограничивает** $\text{rank}(B)$.

**Это — законы природы.** Мы **не можем** их нарушить.

**Следствие:**

- Если $A$ имеет **низкий ранг** — $B$ **не может** иметь **высокий ранг**,
- Если $A$ имеет **малый след** — $B$ **не может** иметь **большой след**.

**MCM подчиняется законам $A$.**

---

### 5.9. Пример: «Чиполлино»

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**Опора:**

- $\widehat{\mathbf{X}}_{AB}$ — **фиксирован** физикой (геометрия ФАР),
- $\widehat{\mathbf{X}}_{DV}^T$ — **фиксирован** физикой (доплеровская обработка),
- $\mathbf{X}_{DA}$ — **содержание** (сигнал).

**Результат:** $\widehat{\mathbf{X}}_{AA}$ **определён однозначно**.

**Без опоры:** если бы $\widehat{\mathbf{X}}_{AB}$ и $\widehat{\mathbf{X}}_{DV}$ были свободны, $\widehat{\mathbf{X}}_{AA}$ можно было бы получить **бесконечным числом способов**.

---

### 5.10. Пример: Transformer

$$
Q = X W_Q.
$$

**Опора:**

- $X$ — **фиксирован** (вход),
- $W_Q$ — **фиксирован** (обучен),
- $Q$ — **вычисляется** напрямую.

**Результат:** $Q$ **определён однозначно**.

**Без опоры:** если бы $X$ и $W_Q$ были свободны, $Q$ можно было бы получить **бесконечным числом способов**.

---

### 5.11. Сводка

| Свойство | Формула |
|----------|---------|
| Множественность | Бесконечно много решений без опоры |
| Опоры | Фиксация $U$ или $V$ |
| Единственность | При фиксированном $U$ или $V$ |
| Минимальность | Размерности $U, V$ минимальны |
| След и ранг $A$ | Не зависят от нас |


## 6. Принцип минимальности
### 6.1. Постановка

MCM-преобразование:

$$
B = U A V^T.
$$

**Дано:** $A \in \mathbb{R}^{m \times n}$, $B \in \mathbb{R}^{p \times q}$.
**Ищем:** $U \in \mathbb{R}^{p \times m}$, $V \in \mathbb{R}^{q \times n}$.

**Вопрос:** какова **минимальная** размерность $U$ и $V$?

**Ответ:** **ровно та**, которая необходима для перевода $A$ в $B$.

**Ни больше. Ни меньше.**

---

### 6.2. Формулировка

> **Принцип 6.1 (Минимальность).**
> Размерности операторов $U$ и $V$ должны быть **минимально необходимыми** для перевода $A$ в $B$:
> $$
> U \in \mathbb{R}^{p \times m}, \quad V \in \mathbb{R}^{q \times n},
> $$
> где:
> - $p$ — число строк $B$,
> - $q$ — число столбцов $B$,
> - $m$ — число строк $A$,
> - $n$ — число столбцов $A$.

**Смысл:**

- $U$ **не может** иметь меньше строк, чем $B$ ($p$),
- $U$ **не может** иметь меньше столбцов, чем $A$ ($m$),
- $V$ **не может** иметь меньше строк, чем $B$ ($q$),
- $V$ **не может** иметь меньше столбцов, чем $A$ ($n$).

**Это — минимум.**

---

### 6.3. Почему минимальность важна

**Три причины:**

1. **Скорость вычислений.**
   - Чем меньше размерность $U$ и $V$, тем **быстрее** умножение.
   - Сложность MCM: $O(p m n + p n q)$.
   - Уменьшение $p$ или $q$ **радикально** ускоряет вычисления.

2. **Память.**
   - $U$ занимает $p \times m$ чисел,
   - $V$ занимает $q \times n$ чисел,
   - Чем меньше размерность, тем **меньше памяти**.

3. **Интерпретируемость.**
   - Чем меньше размерность, тем **проще** понять структуру,
   - Чем больше размерность, тем **сложнее** интерпретировать.

---

### 6.4. Что значит «минимально необходимая»

**Пример 1: сжатие**

- $A \in \mathbb{R}^{3 \times 5}$,
- $B \in \mathbb{R}^{2 \times 4}$.

**Тогда:**

- $U \in \mathbb{R}^{2 \times 3}$ (сжимает 3 строки в 2),
- $V \in \mathbb{R}^{4 \times 5}$ (сжимает 5 столбцов в 4).

**Минимальные размерности:** $2 \times 3$ и $4 \times 5$.

**Пример 2: расширение**

- $A \in \mathbb{R}^{3 \times 5}$,
- $B \in \mathbb{R}^{4 \times 6}$.

**Тогда:**

- $U \in \mathbb{R}^{4 \times 3}$ (расширяет 3 строки в 4),
- $V \in \mathbb{R}^{6 \times 5}$ (расширяет 5 столбцов в 6).

**Минимальные размерности:** $4 \times 3$ и $6 \times 5$.

**Пример 3: сохранение**

- $A \in \mathbb{R}^{3 \times 5}$,
- $B \in \mathbb{R}^{3 \times 5}$.

**Тогда:**

- $U \in \mathbb{R}^{3 \times 3}$ (квадратная),
- $V \in \mathbb{R}^{5 \times 5}$ (квадратная).

**Минимальные размерности:** $3 \times 3$ и $5 \times 5$.

---

### 6.5. Связь с полуортогональностью

**Условие MCM:**

$$
U^T U = I_m, \quad V^T V = I_n.
$$

**Если $U \in \mathbb{R}^{p \times m}$** и $p > m$ — $U$ **расширяет** пространство.

**Если $U \in \mathbb{R}^{p \times m}$** и $p < m$ — $U$ **сжимает** пространство.

**Если $U \in \mathbb{R}^{p \times m}$** и $p = m$ — $U$ **квадратная** ортогональная.

**Принцип минимальности** требует, чтобы:

- $p$ было **ровно** числом строк $B$,
- $m$ было **ровно** числом строк $A$,
- $q$ было **ровно** числом столбцов $B$,
- $n$ было **ровно** числом столбцов $A$.

**Никаких «лишних» размерностей.**

---

### 6.6. След и ранг $A$

**Ты говоришь:**

> След матрицы $A$ и её ранг **от нас не зависят**.

**Согласен.** $A$ — **дана**. Мы **не можем** её изменить.

**Но:**

- $\text{Tr}(A^T A)$ — **сохраняется** при MCM,
- $\text{rank}(A)$ — **ограничивает** $\text{rank}(B)$.

**Это — законы природы.** Мы **не можем** их нарушить.

**Следствие:**

- Если $A$ имеет **низкий ранг** — $B$ **не может** иметь **высокий ранг**,
- Если $A$ имеет **малый след** — $B$ **не может** иметь **большой след**.

**MCM подчиняется законам $A$.**

---

### 6.7. Связь с опорой

**Принцип минимальности** и **теорема о необходимости опоры** — **взаимосвязаны**:

| Свойство | Опора | Минимальность |
|----------|-------|---------------|
| Что фиксируется | $U$ или $V$ | Размерности $U, V$ |
| Зачем | Единственность | Скорость, память |
| Когда | Всегда | Всегда |

**Опора** отвечает на вопрос **«что фиксировать»**.
**Минимальность** отвечает на вопрос **«какой размерности»**.

**Вместе** они делают MCM **корректным** и **эффективным**.

---

### 6.8. Пример: «Чиполлино»

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**Размерности:**

| Матрица | Размерность | Минимальна? |
|---------|-------------|-------------|
| $\widehat{\mathbf{X}}_{AB}$ | $1024 \times 128$ | Да |
| $\widehat{\mathbf{X}}_{DV}^T$ | $128 \times 14500$ | Да |
| $\mathbf{X}_{DA}$ | $14500 \times 1024$ | Да |
| $\widehat{\mathbf{X}}_{AA}$ | $1024 \times 1024$ | Да |

**Смысл:**

- Каждая матрица имеет **минимально необходимую** размерность,
- Никаких «лишних» размерностей,
- Это — **оптимально** по скорости и памяти.

---

### 6.9. Пример: Transformer

$$
Q = X W_Q.
$$

**Размерности:**

| Матрица | Размерность | Минимальна? |
|---------|-------------|-------------|
| $X$ | $n \times 768$ | Да |
| $W_Q$ | $768 \times 64$ | Да |
| $Q$ | $n \times 64$ | Да |

**Смысл:**

- $d_k = 64$ — **минимально необходимая** размерность для запроса,
- Это — **осознанный выбор** архитектуры,
- Больше — **избыточно**, меньше — **недостаточно**.

---

### 6.10. Сводка

| Свойство | Формула |
|----------|---------|
| Размерность $U$ | $p \times m$ |
| Размерность $V$ | $q \times n$ |
| Минимальность | Ровно необходимая |
| Скорость | $O(p m n + p n q)$ |
| Память | $p m + q n$ |
| След $A$ | Сохраняется |
| Ранг $A$ | Ограничивает |

## 7. Закон сохранения субстанции
### 7.1. Формулировка

> **Теорема 7.1 (Сохранение субстанции).**
> Пусть:
> $$
> B = U A V^T,
> $$
> где $A \in \mathbb{R}^{m \times n}$, $U \in \mathbb{R}^{p \times m}$, $V \in \mathbb{R}^{q \times n}$.
> Если $U^T U = I_m$ и $V^T V = I_n$, то:
> $$
> \text{Tr}(B^T B) = \text{Tr}(A^T A).
> $$

**Смысл:**

- **Субстанция** (энергия, объём, информация) **сохраняется**,
- **Форма** меняется,
- **Содержание** перераспределяется.

---

### 7.2. Доказательство

**Шаг 1.** Вычислим $B^T B$:

$$
B^T B = (U A V^T)^T (U A V^T) = V A^T U^T U A V^T.
$$

**Шаг 2.** Используем $U^T U = I_m$:

$$
B^T B = V A^T A V^T.
$$

**Шаг 3.** Вычислим след:

$$
\text{Tr}(B^T B) = \text{Tr}(V A^T A V^T).
$$

**Шаг 4.** Используем свойство следа $\text{Tr}(X Y) = \text{Tr}(Y X)$:

$$
\text{Tr}(V A^T A V^T) = \text{Tr}(A^T A V^T V).
$$

**Шаг 5.** Используем $V^T V = I_n$:

$$
\text{Tr}(A^T A V^T V) = \text{Tr}(A^T A).
$$

**Теорема доказана.** $\blacksquare$

---

### 7.3. Физический смысл

**«Субстанция»** — это **количество**:

- **Энергия** — в физике,
- **Объём** — в гидродинамике,
- **Информация** — в теории информации,
- **Мощность** — в радиолокации.

**Закон сохранения субстанции** говорит:

> **Сколько было — столько и осталось.**
> **Форма изменилась. Количество — нет.**

**Это — аналог законов сохранения в физике:**

| Закон | Что сохраняется |
|-------|-----------------|
| Закон сохранения энергии | Энергия |
| Закон сохранения массы | Масса |
| Закон сохранения импульса | Импульс |
| **Закон сохранения субстанции MCM** | $\text{Tr}(B^T B)$ |

---

### 7.4. Следствие: норма Фробениуса

**Норма Фробениуса:**

$$
\|A\|_F = \sqrt{\text{Tr}(A^T A)}.
$$

**Из теоремы 7.1:**

$$
\|B\|_F = \|A\|_F.
$$

**Смысл:**

- **Норма Фробениуса** сохраняется при MCM,
- **Это — следствие** ортогональности $U$ и $V$,
- **Это — закон**, а не случайность.

---

### 7.5. Следствие: сингулярные числа

**SVD:**

$$
A = U_A \Sigma_A V_A^T, \quad B = U_B \Sigma_B V_B^T.
$$

**Сингулярные числа:**

$$
\sigma_i(A) = \sqrt{\lambda_i(A^T A)}, \quad \sigma_i(B) = \sqrt{\lambda_i(B^T B)}.
$$

**Из теоремы 7.1:**

$$
\sum_i \sigma_i^2(A) = \sum_i \sigma_i^2(B).
$$

**Смысл:**

- **Сумма квадратов сингулярных чисел** сохраняется,
- **Сами сингулярные числа** могут меняться,
- **Их сумма** — **инвариант**.

---

### 7.6. Что НЕ сохраняется

**Закон сохранения субстанции** сохраняет **след**. Но **не сохраняет**:

| Свойство | Сохраняется? |
|----------|--------------|
| След $\text{Tr}(B^T B)$ | **Да** |
| Норма Фробениуса $\|B\|_F$ | **Да** |
| Сумма квадратов $\sigma_i^2$ | **Да** |
| Ранг $\text{rank}(B)$ | **Нет** (может уменьшиться) |
| Сингулярные числа $\sigma_i$ | **Нет** (могут измениться) |
| Собственные числа $\lambda_i$ | **Нет** (могут измениться) |

**Субстанция** сохраняется.
**Структура** — **нет**.

---

### 7.7. Пример: «Чиполлино»

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**Субстанция:**

$$
\text{Tr}(\widehat{\mathbf{X}}_{AA}^H \widehat{\mathbf{X}}_{AA}) = \text{Tr}(\mathbf{X}_{DA}^H \mathbf{X}_{DA}).
$$

**Смысл:**

- **Энергия** когерентного ядра **равна** энергии входного сигнала,
- **Форма** изменилась из $14500 \times 1024$ в $1024 \times 1024$,
- **Количество** — **то же**.

**Это — важно.** Потому что:

- **Шум РЭБ** имеет **большую энергию**,
- **Сигнал БПЛА** имеет **малую энергию**,
- Но **структура** сигнала **когерентна** — это и позволяет его выделить.

---

### 7.8. Пример: Transformer

$$
Q = X W_Q.
$$

**Субстанция:**

$$
\text{Tr}(Q^T Q) = \text{Tr}(W_Q^T X^T X W_Q).
$$

**Если $W_Q^T W_Q = I$:**

$$
\text{Tr}(Q^T Q) = \text{Tr}(X^T X).
$$

**Смысл:**

- **Энергия** запроса **равна** энергии входа,
- **Проекция** $W_Q$ **сохраняет** субстанцию,
- **Это — свойство** полуортогональных проекций.

---

### 7.9. Связь с рангом

**Теорема 7.1** и **теорема 4.1** — **взаимосвязаны**:

| Свойство | Формула |
|----------|---------|
| Субстанция | $\text{Tr}(B^T B) = \text{Tr}(A^T A)$ |
| Ранг | $\text{rank}(B) \le \text{rank}(A)$ |

**Субстанция** сохраняется **точно**.
**Ранг** — **может уменьшиться**.

**Это — не противоречие.** Потому что:

- **Энергия** может **перераспределиться**,
- **Ранг** — это **число ненулевых сингулярных чисел**,
- **Сумма квадратов** сохраняется, но **число слагаемых** может уменьшиться.

**Пример:**

- $A$: $\sigma_1 = 3, \sigma_2 = 4$ → $\sum \sigma_i^2 = 25$, $\text{rank} = 2$,
- $B$: $\sigma_1 = 5, \sigma_2 = 0$ → $\sum \sigma_i^2 = 25$, $\text{rank} = 1$.

**Субстанция** сохранилась.
**Ранг** — **упал**.

---

### 7.10. Практическое значение

**Закон сохранения субстанции** имеет **практическое значение**:

1. **Контроль вычислений.** Если $\text{Tr}(B^T B) \ne \text{Tr}(A^T A)$ — **ошибка**.
2. **Фильтрация шума.** Шум **не сохраняет** субстанцию при MCM (если он некогерентен).
3. **Диагностика.** Если субстанция **изменилась** — значит, $U$ или $V$ **не полуортогональны**.
4. **Проектирование.** Если нужен $B$ с **заданной** субстанцией, нужно обеспечить **сохранение**.

---

### 7.11. Сводка

| Свойство | Формула |
|----------|---------|
| След | $\text{Tr}(B^T B) = \text{Tr}(A^T A)$ |
| Норма Фробениуса | $\|B\|_F = \|A\|_F$ |
| Сумма квадратов $\sigma_i^2$ | $\sum \sigma_i^2(B) = \sum \sigma_i^2(A)$ |
| Ранг | Может уменьшиться |
| Сингулярные числа | Могут измениться |
| Условие | $U^T U = I_m$, $V^T V = I_n$ |


## 8 Взаимодействие — прямая задача
### 8.1. Постановка

**Прямая задача MCM:**

> **Дано:** ядро $A$, левый оператор $U$, правый оператор $V$.
> **Найти:** образ $B$.
> **Формула:**
> $$
> B = U A V^T.
> $$

**Это — взаимодействие.** Матрица $A$ **передаёт** своё содержание матрице $B$ через интерфейс $U, V^T$.

**Единственность:** $B$ **определён однозначно**.

---

### 8.2. Три режима взаимодействия

| Режим | Что дано | Что ищется | Единственность |
|-------|----------|------------|----------------|
| **Прямой** | $A, U, V$ | $B$ | Да, всегда |
| **Обратный с опорой $U$** | $A, B, U$ | $V$ | Да, если $A$ полного ранга |
| **Обратный с опорой $V$** | $A, B, V$ | $U$ | Да, если $A$ полного ранга |

**Прямой режим** — это **вычисление** $B$.
**Обратные режимы** — это **поиск** оператора.

---

### 8.3. Алгоритм прямого вычисления

**Вход:** $A \in \mathbb{R}^{m \times n}$, $U \in \mathbb{R}^{p \times m}$, $V \in \mathbb{R}^{q \times n}$.

**Шаг 1.** Вычислить промежуточную матрицу:

$$
C = U A \in \mathbb{R}^{p \times n}.
$$

**Шаг 2.** Вычислить образ:

$$
B = C V^T \in \mathbb{R}^{p \times q}.
$$

**Сложность:**

- Шаг 1: $O(p m n)$,
- Шаг 2: $O(p n q)$,
- **Итого:** $O(p m n + p n q)$.

---

### 8.4. Проверка условий

**Перед вычислением** нужно проверить:

1. **Размерности:**
   - $U$: $p \times m$,
   - $A$: $m \times n$,
   - $V$: $q \times n$.

2. **Полуортогональность:**
   - $U^T U = I_m$,
   - $V^T V = I_n$.

3. **Ранг:**
   - $\text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V))$.

**Если условия не выполнены** — MCM **некорректен**.

---

### 8.5. Сохранение субстанции

**После вычисления** проверить:

$$
\text{Tr}(B^T B) = \text{Tr}(A^T A).
$$

**Если не выполнено** — ошибка в $U$ или $V$.

**Это — контрольная сумма.** Она гарантирует, что MCM **не потерял** субстанцию.

---

### 8.6. Пример: «Чиполлино»

**Дано:**

| Матрица | Размерность | Роль |
|---------|-------------|------|
| $\widehat{\mathbf{X}}_{AB}$ | $1024 \times 128$ | $U$ |
| $\widehat{\mathbf{X}}_{DV}^T$ | $128 \times 14500$ | $A$ |
| $\mathbf{X}_{DA}$ | $14500 \times 1024$ | $V$ |

**Шаг 1.** Промежуточная матрица:

$$
C = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \in \mathbb{C}^{1024 \times 14500}.
$$

**Шаг 2.** Образ:

$$
\widehat{\mathbf{X}}_{AA} = C \cdot \mathbf{X}_{DA} \in \mathbb{C}^{1024 \times 1024}.
$$

**Результат:** когерентное ядро $\widehat{\mathbf{X}}_{AA}$.

**Проверка:**

$$
\text{Tr}(\widehat{\mathbf{X}}_{AA}^H \widehat{\mathbf{X}}_{AA}) = \text{Tr}(\mathbf{X}_{DA}^H \mathbf{X}_{DA}).
$$

---

### 8.7. Пример: Transformer

**Дано:**

| Матрица | Размерность | Роль |
|---------|-------------|------|
| $X$ | $n \times 768$ | $A$ |
| $W_Q$ | $768 \times 64$ | $V$ |

**Вычисление:**

$$
Q = X W_Q \in \mathbb{R}^{n \times 64}.
$$

**Результат:** запрос $Q$.

**Проверка:**

$$
\text{Tr}(Q^T Q) = \text{Tr}(W_Q^T X^T X W_Q).
$$

**Если $W_Q^T W_Q = I$:**

$$
\text{Tr}(Q^T Q) = \text{Tr}(X^T X).
$$

---

### 8.8. Взаимодействие как передача

**Взаимодействие** — это **передача** содержания $A$ в форму $B$.

**Метафора:**

- $A$ — **1 литр воды**,
- $U$ — **воронка** (перераспределяет по строкам),
- $V$ — **воронка** (перераспределяет по столбцам),
- $B$ — **тот же 1 литр**, но **в другой форме**.

**Субстанция** сохраняется.
**Форма** — **меняется**.

---

### 8.9. Взаимодействие и мутации

**Мутация** — это **изменение** одной из трёх сущностей:

| Мутация | Что меняется |
|---------|--------------|
| $U \to U'$ | Структура строк |
| $A \to A'$ | Содержание |
| $V \to V'$ | Структура столбцов |

**Результат:**

$$
B' = U' A V^T, \quad B' = U A' V^T, \quad B' = U A V'^T.
$$

**Все три мутации наследуются.**

---

### 8.10. Взаимодействие и композиция

**Композиция** — это **цепочка** взаимодействий:

$$
B = U_1 A V_1^T, \quad C = U_2 B V_2^T.
$$

**Тогда:**

$$
C = (U_2 U_1) A (V_2 V_1)^T.
$$

**Смысл:** два взаимодействия **сводятся** к одному.

**Это — наследование интерфейсов.**

---

### 8.11. Сводка

| Свойство | Формула |
|----------|---------|
| Прямая задача | $B = U A V^T$ |
| Сложность | $O(p m n + p n q)$ |
| Условия | $U^T U = I_m$, $V^T V = I_n$ |
| Сохранение | $\text{Tr}(B^T B) = \text{Tr}(A^T A)$ |
| Ранг | $\text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V))$ |
| Композиция | $(U_2 U_1) A (V_2 V_1)^T$ |

## 9. Сравнение — обратная задача
### 9.1. Постановка

**Обратная задача MCM:**

> **Дано:** ядро $A$, образ $B$.
> **Найти:** операторы $U$, $V$.
> **Условие:**
> $$
> B \approx U A V^T.
> $$

**Это — сравнение.** Мы **измеряем**, насколько $A$ и $B$ **похожи** через **оптимальный интерфейс**.

**Единственность:** **нет**, если $U$ и $V$ свободны.

---

### 9.2. Два режима сравнения

| Режим | Что дано | Что ищется | Единственность |
|-------|----------|------------|----------------|
| **С опорой $U$** | $A, B, U$ | $V$ | Да, если $A$ полного ранга |
| **С опорой $V$** | $A, B, V$ | $U$ | Да, если $A$ полного ранга |
| **Без опоры** | $A, B$ | $U, V$ | Нет, бесконечно много |

**С опорой** — задача **корректна**.
**Без опоры** — задача **некорректна** (но это **фича**, а не баг).

---

### 9.3. Сравнение с опорой $U$

**Дано:** $A \in \mathbb{R}^{m \times n}$, $B \in \mathbb{R}^{p \times q}$, $U \in \mathbb{R}^{p \times m}$.

**Ищем:** $V \in \mathbb{R}^{q \times n}$.

**Минимизируем:**

$$
\min_V \|B - U A V^T\|_F^2, \quad V^T V = I_n.
$$

**Решение:**

$$
V^T = A^+ U^T B,
$$

где $A^+$ — псевдообратная матрица.

**Условие единственности:** $A$ полного ранга, $V^T V = I_n$.

**Остаток:**

$$
D_U(A, B) = \|B - U A V^T\|_F.
$$

**Это — мера различия** при фиксированном $U$.

---

### 9.4. Сравнение с опорой $V$

**Дано:** $A \in \mathbb{R}^{m \times n}$, $B \in \mathbb{R}^{p \times q}$, $V \in \mathbb{R}^{q \times n}$.

**Ищем:** $U \in \mathbb{R}^{p \times m}$.

**Минимизируем:**

$$
\min_U \|B - U A V^T\|_F^2, \quad U^T U = I_m.
$$

**Решение:**

$$
U = B V A^+,
$$

где $A^+$ — псевдообратная матрица.

**Условие единственности:** $A$ полного ранга, $U^T U = I_m$.

**Остаток:**

$$
D_V(A, B) = \|B - U A V^T\|_F.
$$

**Это — мера различия** при фиксированном $V$.

---

### 9.5. Сравнение без опоры

**Дано:** $A \in \mathbb{R}^{m \times n}$, $B \in \mathbb{R}^{p \times q}$.

**Ищем:** $U \in \mathbb{R}^{p \times m}$, $V \in \mathbb{R}^{q \times n}$.

**Минимизируем:**

$$
\min_{U, V} \|B - U A V^T\|_F^2, \quad U^T U = I_m, \quad V^T V = I_n.
$$

**Это — обобщённая задача Проcrustes.**

**Решение:** **невыпуклая** задача. Решается **итеративно**:

1. Фиксируем $U$, ищем $V$,
2. Фиксируем $V$, ищем $U$,
3. Повторяем до сходимости.

**Остаток:**

$$
D(A, B) = \min_{U, V} \|B - U A V^T\|_F.
$$

**Это — мера различия** без опоры.

---

### 9.6. Метрика MCM

> **Определение 9.1 (Метрика MCM).**
> $$
> D(A, B) = \min_{U, V} \|B - U A V^T\|_F.
> $$

**Свойства:**

1. **Неотрицательность:** $D(A, B) \ge 0$,
2. **Симметричность:** $D(A, B) = D(B, A)$? **Не всегда** (из-за разной размерности),
3. **Тождество:** $D(A, A) = 0$,
4. **Неравенство треугольника:** $D(A, C) \le D(A, B) + D(B, C)$? **Не всегда**.

**Это — полуметрика**, а не метрика.

---

### 9.7. Связь с взаимодействием

**Взаимодействие** — это **частный случай** сравнения, когда $D = 0$:

$$
D(A, B) = 0 \iff B = U A V^T \text{ для некоторых } U, V.
$$

**Сравнение** — это **обобщение** взаимодействия, когда $D > 0$:

$$
D(A, B) > 0 \iff B \ne U A V^T \text{ для всех } U, V.
$$

**MCM описывает оба случая.**

---

### 9.8. Пример: «Чиполлино»

**Сравнение двух радаров:**

- Радар 1: $\widehat{\mathbf{X}}_{AA1}$,
- Радар 2: $\widehat{\mathbf{X}}_{AA2}$.

**Мера различия:**

$$
D(\widehat{\mathbf{X}}_{AA1}, \widehat{\mathbf{X}}_{AA2}) = \min_{U, V} \|\widehat{\mathbf{X}}_{AA2} - U \widehat{\mathbf{X}}_{AA1} V^T\|_F.
$$

**Смысл:** насколько **похожи** когерентные ядра двух радаров.

**Применение:** сотовая обработка — объединение радаров в сеть.

---

### 9.9. Пример: Transformer

**Сравнение двух нейросетей:**

- Модель 1: $X_1$,
- Модель 2: $X_2$.

**Мера различия:**

$$
D(X_1, X_2) = \min_{U, V} \|X_2 - U X_1 V^T\|_F.
$$

**Смысл:** насколько **похожи** латентные пространства двух моделей.

**Применение:** ReBasin — слияние нейросетей без дообучения.

---

### 9.10. Сравнение и мутации

**Мутация** — это **изменение** одной из трёх сущностей.

**Сравнение** позволяет **обнаружить** мутацию:

| Что сравниваем | Что находим |
|----------------|-------------|
| $A$ и $B$ | Есть ли различие |
| $U$ и $U'$ | Мутация в структуре строк |
| $A$ и $A'$ | Мутация в содержании |
| $V$ и $V'$ | Мутация в структуре столбцов |

**Мера различия** $D(A, B)$ — это **индикатор мутации**.

---

### 9.11. Сводка

| Свойство | Формула |
|----------|---------|
| Сравнение с опорой $U$ | $\min_V \|B - U A V^T\|_F$ |
| Сравнение с опорой $V$ | $\min_U \|B - U A V^T\|_F$ |
| Сравнение без опоры | $\min_{U, V} \|B - U A V^T\|_F$ |
| Метрика | $D(A, B) = \min_{U, V} \|B - U A V^T\|_F$ |
| Связь с взаимодействием | $D = 0 \iff B = U A V^T$ |

## 10. Композиция MCM
### 10.1. Постановка

**Композиция** — это **цепочка** MCM-преобразований:

$$
B = U_1 A V_1^T, \quad C = U_2 B V_2^T.
$$

**Вопрос:** можно ли **свести** две композиции к **одной**?

**Ответ:** да.

---

### 10.2. Теорема о композиции

> **Теорема 10.1 (Композиция MCM).**
> Пусть:
> $$
> B = U_1 A V_1^T, \quad C = U_2 B V_2^T.
> $$
> Тогда:
> $$
> C = (U_2 U_1) A (V_2 V_1)^T.
> $$

**Доказательство:**

Подставим $B$ во второе уравнение:

$$
C = U_2 (U_1 A V_1^T) V_2^T = (U_2 U_1) A (V_1^T V_2^T) = (U_2 U_1) A (V_2 V_1)^T.
$$

**Теорема доказана.** $\blacksquare$

---

### 10.3. Смысл композиции

**Композиция** — это **наследование интерфейсов**:

| Этап | Левый оператор | Ядро | Правый оператор |
|------|----------------|------|-----------------|
| 1 | $U_1$ | $A$ | $V_1$ |
| 2 | $U_2$ | $B$ | $V_2$ |
| **Итог** | $U_2 U_1$ | $A$ | $V_2 V_1$ |

**Смысл:**

- **Ядро** $A$ **сохраняется**,
- **Левые операторы** $U_1, U_2$ **перемножаются**,
- **Правые операторы** $V_1, V_2$ **перемножаются**.

**Это — как наследование признаков.**

---

### 10.4. Свойства композиции

> **Свойство 10.1 (Ассоциативность).**
> $$
> (U_2 U_1) A (V_2 V_1)^T = U_2 (U_1 A V_1^T) V_2^T.
> $$

**Смысл:** порядок **не важен** при ассоциации.

> **Свойство 10.2 (Сохранение полуортогональности).**
> Если $U_1^T U_1 = I$, $U_2^T U_2 = I$, то:
> $$
> (U_2 U_1)^T (U_2 U_1) = U_1^T U_2^T U_2 U_1 = U_1^T U_1 = I.
> $$

**Смысл:** композиция **сохраняет** полуортогональность.

> **Свойство 10.3 (Сохранение субстанции).**
> $$
> \text{Tr}(C^T C) = \text{Tr}(A^T A).
> $$

**Смысл:** композиция **сохраняет** субстанцию.

---

### 10.5. Ранг композиции

**По теореме о ранге:**

$$
\text{rank}(C) \le \min(\text{rank}(U_2 U_1), \text{rank}(A), \text{rank}(V_2 V_1)).
$$

**Известно:**

$$
\text{rank}(U_2 U_1) \le \min(\text{rank}(U_2), \text{rank}(U_1)),
$$
$$
\text{rank}(V_2 V_1) \le \min(\text{rank}(V_2), \text{rank}(V_1)).
$$

**Следствие:** композиция **не увеличивает** ранг. Она **может только уменьшить** его.

---

### 10.6. Пример: «Чиполлино»

**Композиция трёх срезов:**

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**MCM-интерпретация:**

| Этап | Левый оператор | Ядро | Правый оператор |
|------|----------------|------|-----------------|
| 1 | $\widehat{\mathbf{X}}_{AB}$ | $\widehat{\mathbf{X}}_{DV}^T$ | $\mathbf{X}_{DA}$ |
| **Итог** | — | — | $\widehat{\mathbf{X}}_{AA}$ |

**Смысл:** три среза **композируются** в когерентное ядро.

**Ранг:**

$$
\text{rank}(\widehat{\mathbf{X}}_{AA}) \le 128.
$$

---

### 10.7. Пример: Transformer

**Композиция слоёв:**

$$
\text{Head} = \text{softmax}\left( \frac{(X W_Q)(X W_K)^T}{\sqrt{d_k}} \right) (X W_V).
$$

**MCM-интерпретация:**

| Этап | Левый оператор | Ядро | Правый оператор |
|------|----------------|------|-----------------|
| 1 | $X$ | $W_Q$ | — |
| 2 | $X$ | $W_K$ | — |
| 3 | $\text{softmax}$ | $X$ | $W_V$ |
| **Итог** | — | — | $\text{Head}$ |

**Смысл:** несколько MCM-преобразований **композируются** в внимание.

---

### 10.8. Композиция и мутации

**Мутация** может произойти **на любом этапе** композиции:

| Этап | Мутация | Что меняется |
|------|---------|--------------|
| 1 | $U_1 \to U_1'$ | Левый оператор 1 |
| 1 | $A \to A'$ | Ядро |
| 1 | $V_1 \to V_1'$ | Правый оператор 1 |
| 2 | $U_2 \to U_2'$ | Левый оператор 2 |
| 2 | $V_2 \to V_2'$ | Правый оператор 2 |

**Все мутации наследуются.**

**Результат:**

$$
C' = (U_2' U_1') A' (V_2' V_1')^T.
$$

---

### 10.9. Композиция и сравнение

**Сравнение** двух композиций:

$$
D(C_1, C_2) = \min_{U, V} \|C_2 - U C_1 V^T\|_F.
$$

**Смысл:** насколько **похожи** две цепочки MCM-преобразований.

**Применение:**

- Сравнение двух радаров,
- Сравнение двух нейросетей,
- Сравнение двух сигналов.

---

### 10.10. Сводка

| Свойство | Формула |
|----------|---------|
| Композиция | $C = (U_2 U_1) A (V_2 V_1)^T$ |
| Ассоциативность | $(U_2 U_1) A (V_2 V_1)^T = U_2 (U_1 A V_1^T) V_2^T$ |
| Полуортогональность | Сохраняется |
| Субстанция | $\text{Tr}(C^T C) = \text{Tr}(A^T A)$ |
| Ранг | $\text{rank}(C) \le \text{rank}(A)$ |
## 11. Мутации MCM
### 11.1. Постановка

**Мутация** — это **изменение** одной из трёх сущностей MCM:

$$
B = U A V^T.
$$

**Вопрос:** где может произойти мутация, и что она меняет?

**Ответ:** мутация может произойти **в любой** из трёх сущностей. **Все три наследуются.**

---

### 11.2. Три типа мутаций

| Тип | Что меняется | Формула |
|-----|--------------|---------|
| **Мутация в $U$** | Структура строк | $B' = U' A V^T$ |
| **Мутация в $A$** | Содержание | $B' = U A' V^T$ |
| **Мутация в $V$** | Структура столбцов | $B' = U A V'^T$ |

**Все три мутации наследуются.**

**Но последствия разные.**

---

### 11.3. Мутация в $U$

> **Определение 11.1 (Мутация в $U$).**
> $$
> B' = U' A V^T,
> $$
> где $U' \ne U$.

**Что меняется:**

- **Структура строк** $B'$,
- **Пространство наблюдений**,
- **Ранг** $B'$ (может уменьшиться).

**Что сохраняется:**

- **Структура столбцов** $V$,
- **Содержание** $A$,
- **Субстанция** (если $U'^T U' = I$).

**Пример в «Чиполлино»:**

- Антенная решётка **повреждена**,
- $\widehat{\mathbf{X}}_{AB}$ **изменена**,
- $\widehat{\mathbf{X}}_{AA}$ **искажён**.

**Пример в Transformer:**

- $W_Q$ **изменён**,
- $Q$ **искажён**,
- Внимание **искажено**.

---

### 11.4. Мутация в $A$

> **Определение 11.2 (Мутация в $A$).**
> $$
> B' = U A' V^T,
> $$
> где $A' \ne A$.

**Что меняется:**

- **Содержание** $B'$,
- **Ранг** $B'$ (может уменьшиться),
- **Субстанция** если $A' \ne A$.

**Что сохраняется:**

- **Структура строк** $U$,
- **Структура столбцов** $V$.

**Пример в «Чиполлино»:**

- Сигнал **искажён** помехой,
- $\mathbf{X}_{DA}$ **изменена**,
- $\widehat{\mathbf{X}}_{AA}$ **искажён**.

**Пример в Transformer:**

- Вход $X$ **искажён**,
- $Q, K, V$ **искажены**,
- Внимание **искажено**.

---

### 11.5. Мутация в $V$

> **Определение 11.3 (Мутация в $V$).**
> $$
> B' = U A V'^T,
> $$
> где $V' \ne V$.

**Что меняется:**

- **Структура столбцов** $B'$,
- **Пространство признаков**,
- **Ранг** $B'$ (может уменьшиться).

**Что сохраняется:**

- **Структура строк** $U$,
- **Содержание** $A$,
- **Субстанция** (если $V'^T V' = I$).

**Пример в «Чиполлино»:**

- Доплеровская обработка **изменена**,
- $\widehat{\mathbf{X}}_{DV}$ **изменена**,
- $\widehat{\mathbf{X}}_{AA}$ **искажён**.

**Пример в Transformer:**

- $W_K$ **изменён**,
- $K$ **искажён**,
- Внимание **искажено**.

---

### 11.6. Сравнение мутаций

| Свойство | Мутация в $U$ | Мутация в $A$ | Мутация в $V$ |
|----------|-----------------|-----------------|-----------------|
| Что меняется | Структура строк | Содержание | Структура столбцов |
| Что сохраняется | $V, A$ | $U, V$ | $U, A$ |
| Ранг | Может упасть | Может упасть | Может упасть |
| Субстанция | Сохраняется | Может измениться | Сохраняется |
| Наследуется | Да | Да | Да |

**Все три мутации наследуются. Но последствия разные.**

---

### 11.7. Наследование мутаций

**Композиция MCM:**

$$
B = U_1 A V_1^T, \quad C = U_2 B V_2^T.
$$

**Если мутация в $U_1$:**

$$
C' = U_2 (U_1' A V_1^T) V_2^T = (U_2 U_1') A (V_2 V_1)^T.
$$

**Мутация наследуется.**

**Если мутация в $A$:**

$$
C' = U_2 (U_1 A' V_1^T) V_2^T = (U_2 U_1) A' (V_2 V_1)^T.
$$

**Мутация наследуется.**

**Если мутация в $V_1$:**

$$
C' = U_2 (U_1 A V_1'^T) V_2^T = (U_2 U_1) A (V_2 V_1')^T.
$$

**Мутация наследуется.**

**Вывод:** мутация в **любой** сущности **наследуется** при композиции.

---

### 11.8. Мутации и ранг

**Теорема о ранге:**

$$
\text{rank}(B') \le \min(\text{rank}(U'), \text{rank}(A), \text{rank}(V)).
$$

**Если мутация уменьшает ранг $U'$** — ранг $B'$ **может упасть**.

**Аналогично для $A'$ и $V'$.**

**Следствие:** мутация **может уменьшить** ранг $B'$.

**Это — важно.** Потому что:

- **Потеря ранга** — потеря информации,
- **Потеря ранга** — потеря структуры,
- **Потеря ранга** — потеря смысла.

---

### 11.9. Мутации и субстанция

**Закон сохранения субстанции:**

$$
\text{Tr}(B'^T B') = \text{Tr}(A^T A).
$$

**Если мутация в $U$ или $V$** — субстанция **сохраняется**.

**Если мутация в $A$** — субстанция **может измениться**.

**Следствие:**

- Мутация в **структуре** — субстанция **сохраняется**,
- Мутация в **содержании** — субстанция **меняется**.

**Это — важно.** Потому что:

- **Структурные мутации** — **обратимы**,
- **Содержательные мутации** — **необратимы**.

---

### 11.10. Диагностика мутаций

**Как определить, где произошла мутация?**

**Метод 1: Проверка субстанции.**

- Если $\text{Tr}(B'^T B') = \text{Tr}(A^T A)$ — мутация в $U$ или $V$,
- Если $\text{Tr}(B'^T B') \ne \text{Tr}(A^T A)$ — мутация в $A$.

**Метод 2: Проверка ранга.**

- Если $\text{rank}(B') < \text{rank}(A)$ — мутация в $U$, $A$ или $V$,
- Если $\text{rank}(B') = \text{rank}(A)$ — мутация в $U$ или $V$.

**Метод 3: Сравнение с эталоном.**

- Если $B'$ **близка** к $B$ — мутация **мала**,
- Если $B'$ **далека** от $B$ — мутация **велика**.

---

### 11.11. Пример: «Чиполлино»

**Мутация в $\widehat{\mathbf{X}}_{AB}$:**

- Антенная решётка **повреждена**,
- $\widehat{\mathbf{X}}_{AA}$ **искажён**,
- **Диагностика:** проверить геометрию ФАР.

**Мутация в $\widehat{\mathbf{X}}_{DV}^T$:**

- Доплеровская обработка **изменена**,
- $\widehat{\mathbf{X}}_{AA}$ **искажён**,
- **Диагностика:** проверить временную синхронизацию.

**Мутация в $\mathbf{X}_{DA}$:**

- Сигнал **искажён**,
- $\widehat{\mathbf{X}}_{AA}$ **искажён**,
- **Диагностика:** проверить входной сигнал.

---

### 11.12. Пример: Transformer

**Мутация в $W_Q$:**

- Запрос **искажён**,
- Внимание **искажено**,
- **Диагностика:** проверить обучение $W_Q$.

**Мутация в $W_K$:**

- Ключ **искажён**,
- Внимание **искажено**,
- **Диагностика:** проверить обучение $W_K$.

**Мутация в $X$:**

- Вход **искажён**,
- Внимание **искажено**,
- **Диагностика:** проверить входные данные.

---

### 11.13. Сводка

| Свойство | Мутация в $U$ | Мутация в $A$ | Мутация в $V$ |
|----------|-----------------|-----------------|-----------------|
| Формула | $B' = U' A V^T$ | $B' = U A' V^T$ | $B' = U A V'^T$ |
| Что меняется | Структура строк | Содержание | Структура столбцов |
| Субстанция | Сохраняется | Может измениться | Сохраняется |
| Ранг | Может упасть | Может упасть | Может упасть |
| Наследуется | Да | Да | Да |

## 12. Применение: "Чиполлино"
### 12.1. Постановка

Противодроновая система «Чиполлино» — это **работающий пример** MCM.

**Основная формула:**

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**MCM-интерпретация:**

| MCM | «Чиполлино» | Роль |
|-----|-------------|------|
| $U$ | $\widehat{\mathbf{X}}_{AB}$ | Левый оператор |
| $A$ | $\widehat{\mathbf{X}}_{DV}^T$ | Ядро |
| $V$ | $\mathbf{X}_{DA}$ | Правый оператор |
| $B$ | $\widehat{\mathbf{X}}_{AA}$ | Образ |

**Три среза** — это **три сущности** MCM.

---

### 12.2. Физика

**Куб данных:**

$$
\mathbf{Y} \in \mathbb{C}^{M \times K \times L}, \quad M = 1024, \quad K = 14500, \quad L = 128.
$$

**Три среза:**

| Срез | Размерность | Физика |
|------|-------------|--------|
| $\widehat{\mathbf{X}}_{AB}$ | $1024 \times 128$ | Антенны × импульсы |
| $\widehat{\mathbf{X}}_{DV}$ | $14500 \times 128$ | Дальность × импульсы |
| $\mathbf{X}_{DA}$ | $14500 \times 1024$ | Дальность × антенны |

**После БПФ** по времени ось импульсов заменяется на ось частот.

---

### 12.3. Контроль размерностей

**MCM-формула:**

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**Размерности:**

$$
(1024 \times 128) \cdot (128 \times 14500) \cdot (14500 \times 1024) = 1024 \times 1024.
$$

**Проверка:**

| Шаг | Операция | Результат |
|-----|----------|-----------|
| 1 | $\widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T$ | $1024 \times 14500$ |
| 2 | $(\dots) \cdot \mathbf{X}_{DA}$ | $1024 \times 1024$ |

**Всё сходится.**

---

### 12.4. Ранг

**По теореме 4.1:**

$$
\text{rank}(\widehat{\mathbf{X}}_{AA}) \le \min(\text{rank}(\widehat{\mathbf{X}}_{AB}), \text{rank}(\widehat{\mathbf{X}}_{DV}), \text{rank}(\mathbf{X}_{DA})).
$$

**В «Чиполлино»:**

$$
\text{rank}(\widehat{\mathbf{X}}_{AA}) \le 128.
$$

**Смысл:**

- Когерентное ядро **имеет ранг $\le 128$**,
- Это **фундаментальное свойство** «Чиполлино»,
- Именно оно позволяет **отсекать шум** и **фокусировать сигнал**.

**Физически:**

- **Сигнал БПЛА** — **когерентен**, ранг **низкий**,
- **Шум РЭБ** — **некогерентен**, ранг **высокий**,
- **MCM** **отсекает** высокоранговый шум.

---

### 12.5. Сохранение субстанции

**По теореме 7.1:**

$$
\text{Tr}(\widehat{\mathbf{X}}_{AA}^H \widehat{\mathbf{X}}_{AA}) = \text{Tr}(\mathbf{X}_{DA}^H \mathbf{X}_{DA}).
$$

**Смысл:**

- **Энергия** когерентного ядра **равна** энергии входного сигнала,
- **Форма** изменилась из $14500 \times 1024$ в $1024 \times 1024$,
- **Количество** — **то же**.

**Это — важно.** Потому что:

- **Шум РЭБ** имеет **большую энергию**,
- **Сигнал БПЛА** имеет **малую энергию**,
- Но **структура** сигнала **когерентна** — это и позволяет его выделить.

---

### 12.6. Троичная природа

**Три среза** — **равноправны**:

| Сущность | Роль | Что даёт |
|----------|------|----------|
| $\widehat{\mathbf{X}}_{AB}$ | Левый оператор | Структура антенн → дальность |
| $\widehat{\mathbf{X}}_{DV}^T$ | Ядро | Содержание |
| $\mathbf{X}_{DA}$ | Правый оператор | Структура дальность → антенны |

**Ни одна** из трёх **не является главной**.

**Структура** так же важна, как **содержание**.

---

### 12.7. Опора

**В «Чиполлино» опора задана физикой:**

| Оператор | Что фиксирует | Чем фиксирован |
|----------|---------------|----------------|
| $\widehat{\mathbf{X}}_{AB}$ | Геометрия ФАР | Физика антенной решётки |
| $\widehat{\mathbf{X}}_{DV}^T$ | Доплеровская обработка | Физика движения |

**Результат:** $\widehat{\mathbf{X}}_{AA}$ **определён однозначно**.

**Без опоры:** если бы $\widehat{\mathbf{X}}_{AB}$ и $\widehat{\mathbf{X}}_{DV}$ были свободны, $\widehat{\mathbf{X}}_{AA}$ можно было бы получить **бесконечным числом способов**.

---

### 12.8. Мутации

**Мутация в $\widehat{\mathbf{X}}_{AB}$:**

- Антенная решётка **повреждена**,
- $\widehat{\mathbf{X}}_{AA}$ **искажён**,
- **Диагностика:** проверить геометрию ФАР.

**Мутация в $\widehat{\mathbf{X}}_{DV}^T$:**

- Доплеровская обработка **изменена**,
- $\widehat{\mathbf{X}}_{AA}$ **искажён**,
- **Диагностика:** проверить временную синхронизацию.

**Мутация в $\mathbf{X}_{DA}$:**

- Сигнал **искажён**,
- $\widehat{\mathbf{X}}_{AA}$ **искажён**,
- **Диагностика:** проверить входной сигнал.

---

### 12.9. Композиция

**Композиция MCM в «Чиполлино»:**

$$
\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}.
$$

**Это — цепочка** из двух MCM-преобразований:

1. $\widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T$,
2. $(\dots) \cdot \mathbf{X}_{DA}$.

**Смысл:** три среза **композируются** в когерентное ядро.

---

### 12.10. Сравнение радаров

**Сравнение двух радаров:**

- Радар 1: $\widehat{\mathbf{X}}_{AA1}$,
- Радар 2: $\widehat{\mathbf{X}}_{AA2}$.

**Мера различия:**

$$
D(\widehat{\mathbf{X}}_{AA1}, \widehat{\mathbf{X}}_{AA2}) = \min_{U, V} \|\widehat{\mathbf{X}}_{AA2} - U \widehat{\mathbf{X}}_{AA1} V^T\|_F.
$$

**Смысл:** насколько **похожи** когерентные ядра двух радаров.

**Применение:** сотовая обработка — объединение радаров в сеть.

---

### 12.11. Матрица перехода $A_{XX}$

**В разделе 14.5 статьи «Чиполлино»:**

$$
\mathbf{X}_{\text{fusion}} = \widehat{\mathbf{X}}_{AA1} \cdot \left( \mathbf{A}_{XX} \cdot \widehat{\mathbf{X}}_{AA2} \right)^T.
$$

**MCM-интерпретация:**

| MCM | «Чиполлино» | Роль |
|-----|-------------|------|
| $U$ | $\widehat{\mathbf{X}}_{AA1}$ | Левый оператор |
| $A$ | $\mathbf{A}_{XX}$ | Ядро |
| $V$ | $\widehat{\mathbf{X}}_{AA2}$ | Правый оператор |
| $B$ | $\mathbf{X}_{\text{fusion}}$ | Образ |

**Смысл:** матрица перехода $A_{XX}$ — это **ядро** MCM между радарами.

---

### 12.12. Сводка

| Свойство | «Чиполлино» |
|----------|-------------|
| MCM-формула | $\widehat{\mathbf{X}}_{AA} = \widehat{\mathbf{X}}_{AB} \cdot \widehat{\mathbf{X}}_{DV}^T \cdot \mathbf{X}_{DA}$ |
| Три сущности | $\widehat{\mathbf{X}}_{AB}, \widehat{\mathbf{X}}_{DV}, \mathbf{X}_{DA}$ |
| Ранг | $\le 128$ |
| Субстанция | Сохраняется |
| Опора | Физика ФАР |
| Композиция | Два MCM-преобразования |
| Матрица перехода | $A_{XX}$ — ядро MCM |
## 13. Применение: Transformer

### 13.1. Постановка

Transformer — это **второй работающий пример** MCM.

**Основная формула:**

$$
\text{Head} = \text{softmax}\left( \frac{(X W_Q)(X W_K)^T}{\sqrt{d_k}} \right) (X W_V).
$$

**MCM-интерпретация:**

| MCM | Transformer | Роль |
|-----|-------------|------|
| $A$ | $X$ | Ядро (вход) |
| $V$ | $W_Q$ | Правый оператор |
| $B$ | $Q$ | Образ (запрос) |

**Проекции** $Q, K, V$ — это **три MCM-преобразования** одного входа.

---

### 13.2. Три проекции

**Запрос:**

$$
Q = X W_Q.
$$

**Ключ:**

$$
K = X W_K.
$$

**Значение:**

$$
V = X W_V.
$$

**MCM-интерпретация:**

| Проекция | Ядро $A$ | Правый оператор $V$ | Образ $B$ |
|----------|------------|----------------------|-------------|
| $Q$ | $X$ | $W_Q$ | $Q$ |
| $K$ | $X$ | $W_K$ | $K$ |
| $V$ | $X$ | $W_V$ | $V$ |

**Смысл:** вход $X$ **проецируется** в три разных пространства.

---

### 13.3. Контроль размерностей

**Для $Q$:**

$$
\underbrace{(n \times 768)}_{X} \cdot \underbrace{(768 \times 64)}_{W_Q} = \underbrace{(n \times 64)}_{Q}.
$$

**Для $K$:**

$$
\underbrace{(n \times 768)}_{X} \cdot \underbrace{(768 \times 64)}_{W_K} = \underbrace{(n \times 64)}_{K}.
$$

**Для $V$:**

$$
\underbrace{(n \times 768)}_{X} \cdot \underbrace{(768 \times 64)}_{W_V} = \underbrace{(n \times 64)}_{V}.
$$

**Всё сходится.**

---

### 13.4. Ранг

**По теореме 4.1:**

$$
\text{rank}(Q) \le \min(\text{rank}(X), \text{rank}(W_Q)) \le 64.
$$

**Аналогично:**

$$
\text{rank}(K) \le 64, \quad \text{rank}(V) \le 64.
$$

**Смысл:**

- Запрос, ключ и значение **имеют ранг $\le 64$**,
- Это **ограничение архитектуры**,
- Оно **осознанное** — $d_k = 64$ в стандартном Transformer.

---

### 13.5. Сохранение субстанции

**По теореме 7.1:**

$$
\text{Tr}(Q^T Q) = \text{Tr}(W_Q^T X^T X W_Q).
$$

**Если $W_Q^T W_Q = I$:**

$$
\text{Tr}(Q^T Q) = \text{Tr}(X^T X).
$$

**Смысл:**

- **Энергия** запроса **равна** энергии входа,
- **Проекция** $W_Q$ **сохраняет** субстанцию,
- **Это — свойство** полуортогональных проекций.

**Но:** в Transformer $W_Q$ **не полуортогональна** — она **обучается**. Поэтому субстанция **не сохраняется** строго.

**Это — важное отличие** от «Чиполлино».

---

### 13.6. Троичная природа

**Для $Q$:**

| Сущность | Роль | Что даёт |
|----------|------|----------|
| $X$ | Ядро | Содержание (вход) |
| $W_Q$ | Правый оператор | Структура (проекция) |
| $Q$ | Образ | Запрос |

**Для $K$:**

| Сущность | Роль | Что даёт |
|----------|------|----------|
| $X$ | Ядро | Содержание (вход) |
| $W_K$ | Правый оператор | Структура (проекция) |
| $K$ | Образ | Ключ |

**Для $V$:**

| Сущность | Роль | Что даёт |
|----------|------|----------|
| $X$ | Ядро | Содержание (вход) |
| $W_V$ | Правый оператор | Структура (проекция) |
| $V$ | Образ | Значение |

**Смысл:** один вход $X$ — **три разных образа**.

---

### 13.7. Опора

**В Transformer опора задана архитектурой:**

| Оператор | Что фиксирует | Чем фиксирован |
|----------|---------------|----------------|
| $X$ | Вход | Данные |
| $W_Q, W_K, W_V$ | Проекции | Обучение |
| $d_k$ | Размерность | Архитектура |

**Результат:** $Q, K, V$ **определены однозначно**.

**Без опоры:** если бы $X$ и $W_Q$ были свободны, $Q$ можно было бы получить **бесконечным числом способов**.

---

### 13.8. Мутации

**Мутация в $W_Q$:**

- Запрос **искажён**,
- Внимание **искажено**,
- **Диагностика:** проверить обучение $W_Q$.

**Мутация в $W_K$:**

- Ключ **искажён**,
- Внимание **искажено**,
- **Диагностика:** проверить обучение $W_K$.

**Мутация в $X$:**

- Вход **искажён**,
- Внимание **искажено**,
- **Диагностика:** проверить входные данные.

---

### 13.9. Композиция

**Композиция MCM в Transformer:**

$$
\text{Head} = \text{softmax}\left( \frac{(X W_Q)(X W_K)^T}{\sqrt{d_k}} \right) (X W_V).
$$

**Это — цепочка** из нескольких MCM-преобразований:

1. $Q = X W_Q$,
2. $K = X W_K$,
3. $V = X W_V$,
4. $\text{Attention} = \text{softmax}\left( \frac{Q K^T}{\sqrt{d_k}} \right) V$.

**Смысл:** вход $X$ **композируется** в внимание.

---

### 13.10. Сравнение нейросетей

**Сравнение двух моделей:**

- Модель 1: $X_1$,
- Модель 2: $X_2$.

**Мера различия:**

$$
D(X_1, X_2) = \min_{U, V} \|X_2 - U X_1 V^T\|_F.
$$

**Смысл:** насколько **похожи** латентные пространства двух моделей.

**Применение:** ReBasin — слияние нейросетей без дообучения.

---

### 13.11. ReBasin

**ReBasin** — это **слияние нейросетей** через **перестановку нейронов**.

**MCM-интерпретация:**

$$
W_2 = U W_1 V^T,
$$

где:

- $W_1, W_2$ — веса двух моделей,
- $U, V$ — перестановочные матрицы.

**Смысл:** веса одной модели **проецируются** в веса другой.

**Это — MCM-преобразование.**

---

### 13.12. Отличие от «Чиполлино»

| Свойство | «Чиполлино» | Transformer |
|----------|-------------|-------------|
| Полуортогональность | Строгая | Нет (обучаемые веса) |
| Сохранение субстанции | Строгое | Приблизительное |
| Опора | Физика | Архитектура + обучение |
| Ранг | $\le 128$ | $\le 64$ |
| Композиция | Два MCM | Много MCM |

**«Чиполлино»** — это **строгий** MCM.
**Transformer** — это **приблизительный** MCM.

---

### 13.13. Сводка

| Свойство | Transformer |
|----------|-------------|
| MCM-формула | $Q = X W_Q$, $K = X W_K$, $V = X W_V$ |
| Три сущности | $X, W_Q, Q$ |
| Ранг | $\le 64$ |
| Субстанция | Приблизительно сохраняется |
| Опора | Архитектура + обучение |
| Композиция | Много MCM |
| Применение | ReBasin |

## 14. Метрика MCM
### 14.1. Постановка

**Сравнение** двух матриц $A$ и $B$ разной размерности:

$$
D(A, B) = \min_{U, V} \|B - U A V^T\|_F.
$$

**Вопрос:** является ли $D(A, B)$ **метрикой**?

**Ответ:** **нет**, это **полуметрика**. Но она **полезна**.

---

### 14.2. Определение

> **Определение 14.1 (Мера различия MCM).**
> $$
> D(A, B) = \min_{U, V} \|B - U A V^T\|_F,
> $$
> где $U^T U = I_m$, $V^T V = I_n$.

**Смысл:** $D(A, B)$ — это **минимальное расстояние** между $B$ и всеми возможными образами $A$.

**Если $D(A, B) = 0$** — $A$ и $B$ **эквивалентны** с точностью до интерфейса.

**Если $D(A, B) > 0$** — $A$ и $B$ **различны**.

---

### 14.3. Свойства

> **Свойство 14.1 (Неотрицательность).**
> $$
> D(A, B) \ge 0.
> $$

> **Свойство 14.2 (Тождество).**
> $$
> D(A, A) = 0.
> $$

> **Свойство 14.3 (Симметричность).**
> $$
> D(A, B) = D(B, A)?
> $$
> **Не всегда.** Из-за разной размерности $A$ и $B$.

> **Свойство 14.4 (Неравенство треугольника).**
> $$
> D(A, C) \le D(A, B) + D(B, C)?
> $$
> **Не всегда.**

**Вывод:** $D(A, B)$ — это **полуметрика**, а не метрика.

---

### 14.4. Почему не метрика

**Проблема 1: Симметричность.**

Если $A \in \mathbb{R}^{3 \times 5}$, $B \in \mathbb{R}^{4 \times 6}$, то:

$$
D(A, B) = \min_{U \in \mathbb{R}^{4 \times 3}, V \in \mathbb{R}^{6 \times 5}} \|B - U A V^T\|_F,
$$

$$
D(B, A) = \min_{U \in \mathbb{R}^{3 \times 4}, V \in \mathbb{R}^{5 \times 6}} \|A - U B V^T\|_F.
$$

**Это — разные задачи.** Разные размерности $U$ и $V$.

**Проблема 2: Неравенство треугольника.**

Из-за разной размерности **не всегда** выполняется.

**Проблема 3: Тождество.**

$D(A, A) = 0$ **только если** $A$ можно получить из $A$ через MCM. Но $A = I A I^T$ — **всегда**. Значит, $D(A, A) = 0$.

**Тождество выполняется.**

---

### 14.5. Нормировка

**Чтобы сравнивать** $D(A, B)$ для разных пар, нужно **нормировать**:

$$
\tilde{D}(A, B) = \frac{D(A, B)}{\max(\|A\|_F, \|B\|_F)}.
$$

**Свойства:**

- $\tilde{D}(A, B) \in [0, 1]$,
- $\tilde{D}(A, B) = 0$ — эквивалентны,
- $\tilde{D}(A, B) = 1$ — максимально различны.

**Это — нормированная мера различия.**

---

### 14.6. Связь с рангом

**По теореме о ранге:**

$$
\text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V)).
$$

**Если $\text{rank}(B) > \text{rank}(A)$** — $D(A, B) > 0$.

**Следствие:**

$$
\text{rank}(B) > \text{rank}(A) \implies D(A, B) > 0.
$$

**Но обратное неверно:** $D(A, B) > 0$ **не означает** $\text{rank}(B) > \text{rank}(A)$.

---

### 14.7. Связь с субстанцией

**По теореме о сохранении субстанции:**

$$
\text{Tr}(B^T B) = \text{Tr}(A^T A).
$$

**Если $\text{Tr}(B^T B) \ne \text{Tr}(A^T A)$** — $D(A, B) > 0$.

**Следствие:**

$$
\text{Tr}(B^T B) \ne \text{Tr}(A^T A) \implies D(A, B) > 0.
$$

**Но обратное неверно:** $D(A, B) > 0$ **не означает** $\text{Tr}(B^T B) \ne \text{Tr}(A^T A)$.

---

### 14.8. Вычисление

**Для фиксированного $U$:**

$$
D_U(A, B) = \min_V \|B - U A V^T\|_F.
$$

**Решение:**

$$
V^T = A^+ U^T B.
$$

**Для фиксированного $V$:**

$$
D_V(A, B) = \min_U \|B - U A V^T\|_F.
$$

**Решение:**

$$
U = B V A^+.
$$

**Без опоры:**

$$
D(A, B) = \min_{U, V} \|B - U A V^T\|_F.
$$

**Решение:** **итеративное** (чередующиеся проекции).

---

### 14.9. Алгоритм вычисления

**Вход:** $A \in \mathbb{R}^{m \times n}$, $B \in \mathbb{R}^{p \times q}$.

**Шаг 1.** Инициализировать $U_0, V_0$ случайно.

**Шаг 2.** Повторять до сходимости:

1. Фиксировать $U_k$, найти $V_{k+1}$:
   $$
   V_{k+1}^T = A^+ U_k^T B.
   $$

2. Фиксировать $V_{k+1}$, найти $U_{k+1}$:
   $$
   U_{k+1} = B V_{k+1} A^+.
   $$

3. Проверить сходимость:
   $$
   \|U_{k+1} - U_k\|_F + \|V_{k+1} - V_k\|_F < \varepsilon.
   $$

**Шаг 3.** Вычислить $D(A, B)$:
   $$
   D(A, B) = \|B - U_{k+1} A V_{k+1}^T\|_F.
   $$

**Сложность:** $O(K \cdot (p m n + p n q))$, где $K$ — число итераций.

---

### 14.10. Пример: «Чиполлино»

**Сравнение двух радаров:**

- Радар 1: $\widehat{\mathbf{X}}_{AA1}$,
- Радар 2: $\widehat{\mathbf{X}}_{AA2}$.

**Мера различия:**

$$
D(\widehat{\mathbf{X}}_{AA1}, \widehat{\mathbf{X}}_{AA2}) = \min_{U, V} \|\widehat{\mathbf{X}}_{AA2} - U \widehat{\mathbf{X}}_{AA1} V^T\|_F.
$$

**Смысл:** насколько **похожи** когерентные ядра двух радаров.

**Применение:** сотовая обработка — объединение радаров в сеть.

---

### 14.11. Пример: Transformer

**Сравнение двух нейросетей:**

- Модель 1: $X_1$,
- Модель 2: $X_2$.

**Мера различия:**

$$
D(X_1, X_2) = \min_{U, V} \|X_2 - U X_1 V^T\|_F.
$$

**Смысл:** насколько **похожи** латентные пространства двух моделей.

**Применение:** ReBasin — слияние нейросетей без дообучения.

---

### 14.12. Сводка

| Свойство                 | Формула                                              |
| ------------------------ | ---------------------------------------------------- |
| Мера различия            | $D(A, B) = \min_{U, V} \|B - U A V^T\|_F$            |
| Нормировка               | $\tilde{D}(A, B) = D(A, B) / \max(\|A\|_F, \|B\|_F)$ |
| Неотрицательность        | $D(A, B) \ge 0$                                      |
| Тождество                | $D(A, A) = 0$                                        |
| Симметричность           | Не всегда                                            |
| Неравенство треугольника | Не всегда                                            |
| Тип                      | Полуметрика                                          |

## 15. Заключение
### 15.1. Что сделано

В работе предложен **MCM (Matrix Composition Method)** — метод композиции матриц разной размерности через интерфейс.

**Основная формула:**

$$
B = U A V^T.
$$

**Три сущности:**

| Сущность | Роль |
|----------|------|
| $U$ | Левый оператор (структура строк) |
| $A$ | Ядро (содержание) |
| $V$ | Правый оператор (структура столбцов) |

**Результат:** $B$ — образ.

---

### 15.2. Основные результаты

1. **Определение MCM** — $B = U A V^T$, $U^T U = I_m$, $V^T V = I_n$.

2. **Троичная природа** — $U, V, A$ равноправны. Структура так же важна, как содержание.

3. **Теорема о ранге** — $\text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V))$.

4. **Теорема о необходимости опоры** — без фиксации $U$ или $V$ решение не единственно. **Это не баг, а фича.** Множественность — пространство возможностей.

5. **Принцип минимальности** — размерности $U$ и $V$ минимально необходимы.

6. **Закон сохранения субстанции** — $\text{Tr}(B^T B) = \text{Tr}(A^T A)$.

7. **Взаимодействие** — прямая задача. $B = U A V^T$.

8. **Сравнение** — обратная задача. $D(A, B) = \min_{U, V} \|B - U A V^T\|_F$.

9. **Композиция** — $C = (U_2 U_1) A (V_2 V_1)^T$. Наследование интерфейсов.

10. **Мутации** — в $U$, $A$ или $V$. Все три наследуются.

11. **Применение: «Чиполлино»** — работающий пример MCM.

12. **Применение: Transformer** — работающий пример MCM.

13. **Метрика MCM** — $D(A, B)$ — полуметрика.

---

### 15.3. Что нового

| Свойство | Существующие подходы | MCM |
|----------|---------------------|-----|
| Матриц | 2 | 2 |
| Размерности | Совпадают (Проcrustes) | **Разные** |
| Операторов | 1 | **2** |
| Опора | Нет | **Есть** |
| Сохранение субстанции | Следствие | **Закон** |
| Композиция | Нет | **Есть** |
| Мутации | Нет | **Есть** |
| Троичная природа | Нет | **Есть** |

**MCM обобщает** Проcrustes, Subspace Alignment, GCCA.

---

### 15.4. Применения

1. **Радиолокация** — «Чиполлино», сотовая обработка, сравнение радаров.

2. **Нейросети** — Transformer, ReBasin, слияние моделей.

3. **Transfer learning** — перенос признаков между доменами.

4. **Биоинформатика** — сравнение белков, рекомбинация.

5. **Кристаллография** — переходы между решётками.

6. **Анализ данных** — сравнение латентных пространств.

---

### 15.5. Открытые вопросы

1. **Полная метрика.** $D(A, B)$ — полуметрика. Как сделать её метрикой?

2. **Оптимальная опора.** Какую матрицу фиксировать — $U$ или $V$?

3. **Сложность.** Как ускорить вычисление $D(A, B)$?

4. **Устойчивость.** Как MCM ведёт себя при шуме?

5. **Обобщение.** Можно ли обобщить MCM на тензоры?

6. **Связь с физикой.** Как MCM связан с интерференцией волн?

---

### 15.6. Что дальше

**Теоретические направления:**

1. **Полная метрика MCM** — сделать $D(A, B)$ метрикой.

2. **Обобщение на тензоры** — $B = U \mathcal{A} V^T$.

3. **Связь с римановой геометрией** — метрика на многообразии.

4. **Связь с теорией информации** — энтропия MCM.

**Прикладные направления:**

1. **«Чиполлино»** — сотовая обработка, 6D-синтез.

2. **Transformer** — ReBasin, слияние моделей.

3. **Transfer learning** — domain adaptation.

4. **Биоинформатика** — анализ геномов.

**Экспериментальные направления:**

1. **Численные эксперименты** — проверка теорем.

2. **Сравнение с Проcrustes** — показать преимущества.

3. **Сравнение с GCCA** — показать преимущества.

4. **Применение к реальным данным** — «Чиполлино», Transformer.

---

### 15.7. Итог

**MCM — это:**

- **Язык** описания композиции матриц разной размерности,
- **Мост** между классической линейной алгеброй и современным машинным обучением,
- **Инструмент** для радиолокации, нейросетей, transfer learning.

**MCM — не вместо. MCM — над.**

**Формула:**

$$
B = U A V^T.
$$

**Закон:**

$$
\text{Tr}(B^T B) = \text{Tr}(A^T A).
$$

**Теорема:**

$$
\text{rank}(B) \le \min(\text{rank}(U), \text{rank}(A), \text{rank}(V)).
$$

**Это — MCM.**

## 16. Приложение аналогии
### 16.1. Зачем аналогии

MCM — это **математика**. Но математика **суха**. Аналогии помогают **понять** и **запомнить**.

**Важно:** аналогии **не являются** частью теории. Они — **иллюстрации**.

---

### 16.2. Аналогия 1: Печать и оттиск

| MCM | Печать |
|-----|--------|
| $U$ | Форма (клише) |
| $A$ | Краска |
| $V$ | Бумага |
| $B$ | Оттиск |

**Смысл:**

- **Форма** определяет, **как** краска ляжет на бумагу,
- **Краска** — это **содержание**,
- **Бумага** — это **структура**,
- **Оттиск** — **результат**.

**Плюсы:** точно, просто, без биологии.
**Минусы:** нет наследования.

---

### 16.3. Аналогия 2: Линза и изображение

| MCM | Линза |
|-----|-------|
| $U$ | Первая линза |
| $A$ | Предмет |
| $V$ | Вторая линза |
| $B$ | Изображение |

**Смысл:**

- **Предмет** $A$ проецируется через **линзы** $U, V$,
- **Изображение** $B$ — результат проекции,
- **Линзы** не меняют предмет, но меняют изображение.

**Плюсы:** точно, физично.
**Минусы:** нет мутаций.

---

### 16.4. Аналогия 3: Фильтр и сигнал

| MCM | Фильтр |
|-----|--------|
| $U$ | Входной фильтр |
| $A$ | Сигнал |
| $V$ | Выходной фильтр |
| $B$ | Отфильтрованный сигнал |

**Смысл:**

- **Сигнал** $A$ проходит через **фильтры** $U, V$,
- **Выходной сигнал** $B$ — результат фильтрации,
- **Фильтры** не меняют энергию, но меняют форму.

**Плюсы:** точно, технично, близко к «Чиполлино».
**Минусы:** нет наследования.

---

### 16.5. Аналогия 4: ДНК

| MCM | ДНК |
|-----|-----|
| $U$ | 23 хромосомы отца |
| $V$ | 23 хромосомы матери |
| $A$ | Содержание (гены) |
| $B$ | Ребёнок |

**Смысл:**

- **Отец** и **мать** дают **структуру**,
- **Содержание** — **третья** сущность,
- **Ребёнок** — **результат** композиции.

**Плюсы:** наглядно, есть наследование, есть мутации.
**Минусы:** тянет биологию.

**Важно:** ДНК — это **аналогия**, а не **теория**. В MCM нет «генов», «хромосом» и «клеток». Есть $U, V, A$.

---

### 16.6. Аналогия 5: Камни в воде

| MCM | Камни в воде |
|-----|--------------|
| $A$ | Камень 1 |
| $B$ | Камень 2 |
| $U, V$ | Интерфейс |
| $X_{\text{res}}$ | Волновая картина |

**Смысл:**

- Два **камня** падают в воду,
- **Волны** интерферируют,
- **Результат** — единая волновая картина.

**Плюсы:** красиво, физично.
**Минусы:** нет наследования.

---

### 16.7. Аналогия 6: Отец, мать, ребёнок

| MCM | Семья |
|-----|-------|
| $U$ | Отец |
| $V$ | Мать |
| $A$ | Содержание |
| $B$ | Ребёнок |

**Смысл:**

- **Отец** даёт **структуру строк**,
- **Мать** даёт **структуру столбцов**,
- **Содержание** — **третья** сущность,
- **Ребёнок** — **результат**.

**Плюсы:** наглядно, есть наследование, есть мутации.
**Минусы:** антропоморфизм.

---

### 16.8. Сравнение аналогий

| Аналогия | Наглядна | Наследование | Мутации | Без биологии |
|----------|----------|--------------|---------|--------------|
| Печать | Да | Нет | Нет | Да |
| Линза | Да | Нет | Нет | Да |
| Фильтр | Да | Нет | Нет | Да |
| ДНК | Да | Да | Да | Нет |
| Камни | Да | Нет | Нет | Да |
| Семья | Да | Да | Да | Нет |

**Лучшие аналогии:**

- **Для объяснения:** печать, линза, фильтр.
- **Для интуиции:** ДНК, семья.
- **Для физики:** камни в воде.

---

### 16.9. Как использовать аналогии

**В статье:**

- **Основной текст** — без аналогий,
- **Приложение** — с аналогиями,
- **Доклад** — с аналогиями.

**В разговоре:**

- **С коллегами** — без аналогий,
- **С не-специалистами** — с аналогиями.

**В обучении:**

- **Студентам** — с аналогиями,
- **Аспирантам** — без аналогий.

---

### 16.10. Сводка

| Аналогия | Что иллюстрирует |
|----------|------------------|
| Печать | Передачу формы |
| Линза | Проекцию |
| Фильтр | Обработку сигнала |
| ДНК | Наследование и мутации |
| Камни | Интерференцию |
| Семья | Троичную природу |

**Все аналогии — вспомогательные.**
**Теория — MCM.**


## Литература и источники

### Фундаментальные работы по линейной алгебре

1. Golub G.H., Van Loan C.F. Matrix Computations. — 4th ed. — Johns Hopkins University Press, 2013.
2. Horn R.A., Johnson C.R. Matrix Analysis. — 2nd ed. — Cambridge University Press, 2012.
3. Strang G. Linear Algebra and Its Applications. — 5th ed. — Cengage Learning, 2016.

### Сингулярное разложение и его приложения

4. Stewart G.W. On the Early History of the Singular Value Decomposition // SIAM Review. — 1993. — Vol. 35, No. 4. — P. 551–566.
5. Klema V.C., Laub A.J. The Singular Value Decomposition: Its Computation and Some Applications // IEEE Transactions on Automatic Control. — 1980. — Vol. 25, No. 2. — P. 164–176.

### Задача Проcrustes

6. Schönemann P.H. A Generalized Solution of the Orthogonal Procrustes Problem // Psychometrika. — 1966. — Vol. 31, No. 1. — P. 1–10.
7. Kabsch W. A Solution for the Best Rotation to Relate Two Sets of Vectors // Acta Crystallographica Section A. — 1976. — Vol. 32, No. 5. — P. 922–923.
8. Umeyama S. Least-Squares Estimation of Transformation Parameters Between Two Point Patterns // IEEE Transactions on Pattern Analysis and Machine Intelligence. — 1991. — Vol. 13, No. 4. — P. 376–380.
9. Gower J.C., Dijksterhuis G.B. Procrustes Problems. — Oxford University Press, 2004.

### Subspace Alignment и Domain Adaptation

10. Fernando B., Habrard A., Sebban M., Tuytelaars T. Unsupervised Visual Domain Adaptation Using Subspace Alignment // ICCV. — 2013. — P. 2960–2967.
11. Gopalan R., Li R., Chellappa R. Domain Adaptation for Object Recognition: An Unsupervised Approach // ICCV. — 2011. — P. 999–1006.
12. Chen B., Lam W., Tsang I.W., Wong T.L. Extracting Discriminative Concepts for Domain Adaptation in Text Mining // KDD. — 2009.

### Канонический корреляционный анализ

13. Hotelling H. Relations Between Two Sets of Variates // Biometrika. — 1936. — Vol. 28, No. 3/4. — P. 321–377.
14. Hardoon D.R., Szedmak S., Shawe-Taylor J. Canonical Correlation Analysis: An Overview with Application to Learning Methods // Neural Computation. — 2004. — Vol. 16, No. 12. — P. 2639–2664.
15. Bach F.R., Jordan M.I. Kernel Independent Component Analysis // Journal of Machine Learning Research. — 2002. — Vol. 3. — P. 1–48.
16. Tenenhaus A., Tenenhaus M. Regularized Generalized Canonical Correlation Analysis // Psychometrika. — 2011. — Vol. 76, No. 2. — P. 257–284.

### Riemannian Manifolds и Fréchet Mean

17. Pennec X., Fillard P., Ayache N. A Riemannian Framework for Tensor Computing // International Journal of Computer Vision. — 2006. — Vol. 66, No. 1. — P. 41–66.
18. Fletcher P.T., Joshi S. Riemannian Geometry for the Statistical Analysis of Diffusion Tensor Data // Signal Processing. — 2007. — Vol. 87, No. 2. — P. 250–262.
19. Barachant A., Bonnet S., Congedo M., Jutten C. Multiclass Brain-Computer Interface Classification by Riemannian Geometry // IEEE Transactions on Biomedical Engineering. — 2012. — Vol. 59, No. 4. — P. 920–928.
20. Congedo M., Barachant A., Bhatia R. Riemannian Geometry for EEG-Based Brain-Computer Interfaces: A Primer and a Review // Brain-Computer Interfaces. — 2017. — Vol. 4, No. 3. — P. 155–174.

### Transfer Learning и Domain Adaptation

21. Pan S.J., Yang Q. A Survey on Transfer Learning // IEEE Transactions on Knowledge and Data Engineering. — 2010. — Vol. 22, No. 10. — P. 1345–1359.
22. Zhuang F., Qi Z., Duan K. et al. A Comprehensive Survey on Transfer Learning // Proceedings of the IEEE. — 2021. — Vol. 109, No. 1. — P. 43–76.
23. Wang M., Deng W. Deep Visual Domain Adaptation: A Survey // Neurocomputing. — 2018. — Vol. 312. — P. 135–153.

### Operator-Valued Kernels

24. Micchelli C.A., Pontil M. On Learning Vector-Valued Functions // Neural Computation. — 2005. — Vol. 17, No. 1. — P. 177–204.
25. Caponnetto A., De Vito E. Optimal Rates for the Regularized Least-Squares Algorithm // Foundations of Computational Mathematics. — 2007. — Vol. 7, No. 3. — P. 331–368.
26. Álvarez M.A., Rosasco L., Lawrence N.D. Kernels for Vector-Valued Functions: A Review // Foundations and Trends in Machine Learning. — 2012. — Vol. 4, No. 3. — P. 195–266.

### Transformer и механизм внимания

27. Vaswani A., Shazeer N., Parmar N. et al. Attention is All You Need // NeurIPS. — 2017. — P. 5998–6008.
28. Dosovitskiy A., Beyer L., Kolesnikov A. et al. An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale // ICLR. — 2021.
29. Radford A., Kim J.W., Hallacy C. et al. Learning Transferable Visual Models From Natural Language Supervision // ICML. — 2021. — P. 8748–8763.

### Слияние нейросетей и ReBasin

30. Singh S.P., Jaggi M. Model Fusion via Optimal Transport // NeurIPS. — 2020.
31. Entezari R., Wortsman M., Ilharco G. et al. The Role of Permutation Invariance in Linear Mode Connectivity of Neural Networks // ICLR. — 2022.
32. Ainsworth S.K., Hayase J., Srinivasa R. Git Re-Basin: Merging Models Modulo Permutation Symmetries // ICLR. — 2023.

### Радиолокация и обработка сигналов ФАР

33. Ширман Я.Д. Теория и техника обработки радиолокационной информации на фоне помех. — М.: Советское радио, 1973.
34. Уидроу Б., Стирнз С. Адаптивная обработка сигналов. — М.: Мир, 1989.
35. Monzingo R.A., Miller T.W. Introduction to Adaptive Arrays. — SciTech Publishing, 2004.
36. Van Trees H.L. Optimum Array Processing. — Wiley, 2002.

### Проект «Чиполлино»

37. Абдуллин Р., DeepSeek, Gemini. Противодроновая система «Чиполлино»: Тензорно-комбинаторный механизм внимания для когерентной обработки сигналов ФАР в частотной области. — 2026.

### Работы по геометрии латентных пространств

38. Kornblith S., Norouzi M., Lee H., Hinton G. Similarity of Neural Network Representations Revisited // ICML. — 2019. — P. 3519–3529.
39. Raghu M., Gilmer J., Yosinski J., Sohl-Dickstein J. SVCCA: Singular Vector Canonical Correlation Analysis for Deep Learning Dynamics and Interpretability // NeurIPS. — 2017. — P. 6076–6085.
40. Morcos A., Raghu M., Bengio S. Insights on Representational Similarity in Neural Networks with Canonical Correlation // NeurIPS. — 2018. — P. 5727–5736.
